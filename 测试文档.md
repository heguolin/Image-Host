# NEON.IMG 测试文档

> **文档版本**：v1.4
> **编写日期**：2026-05-29
> **维护者**：hgl
> **适用版本**：NEON.IMG v1.4

---

## 第一章 测试需求与范围

### 1.1 系统介绍

#### 1.1.1 系统描述

NEON.IMG 是一个基于 Node.js + Express 的轻量级图床服务，采用赛博朋克霓虹美学 UI 设计。系统支持多用户注册、游客匿名上传、图片审核、在线预览、一键复制链接、批量管理、标签分类、数据统计看板等功能。

**技术架构**：

| 层 | 技术 | 说明 |
|------|------|------|
| 运行时 | Node.js v22.21.0 | JavaScript 运行环境 |
| 后端框架 | Express 5.2.1 | RESTful API 服务 |
| 文件上传 | Multer 2.1.1 | multipart/form-data 解析 |
| 图片处理 | sharp 0.33.x | 缩略图生成、格式转换 |
| 数据存储 | sql.js 1.10.x（SQLite WASM） | 元数据持久化 |
| 鉴权 | jsonwebtoken 9.0.x + bcryptjs 2.4.x | JWT 会话 + API Token 双轨 |
| 前端 | 原生 HTML + CSS + JS | 无框架、无构建工具 |
| 部署 | PM2 + Nginx | 进程守护 + 反向代理 |

#### 1.1.2 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                    客户端 (Browser)                       │
│  ┌──────────┐  ┌───────────┐  ┌────────────────────┐    │
│  │ index.html│  │ api-docs  │  │ PicGo / Typora     │    │
│  │ + app.js  │  │ .html     │  │ / curl (外部工具)  │    │
│  │ + style.css│ │           │  │                    │    │
│  └─────┬─────┘  └─────┬─────┘  └────────┬───────────┘    │
└────────┼──────────────┼─────────────────┼───────────────┘
         │              │                 │
         │   HTTP/HTTPS │                 │
         ▼              ▼                 ▼
┌─────────────────────────────────────────────────────────┐
│                    Nginx (反向代理)                       │
│              :80/:443 → localhost:3000                   │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────┐
│                   Express 5.2.1 (PM2)                    │
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐          │
│  │ 鉴权层    │  │ 限流层    │  │ 审核管线     │          │
│  │ JWT+Token│  │ rateLimit │  │ 预检→AI→人工 │          │
│  └─────┬────┘  └─────┬────┘  └──────┬───────┘          │
│        │             │              │                    │
│  ┌─────┴─────────────┴──────────────┴───────┐           │
│  │              路由层 (routes/)               │           │
│  │  upload.js │ auth.js │ admin.js           │           │
│  └─────────────────┬─────────────────────────┘           │
│                    │                                      │
│  ┌─────────────────┴─────────────────────────┐           │
│  │           存储层 (utils/)                   │           │
│  │  meta.js / userMeta.js → db.js (sql.js)  │           │
│  │  + uploads/ (文件系统)                     │           │
│  └───────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────┘
```

#### 1.1.3 功能模块图

```
NEON.IMG v1.4
│
├── 1. 上传模块
│   ├── 拖拽上传（dropzone）
│   ├── 点击上传（file input）
│   ├── 粘贴上传（Ctrl+V / clipboard）
│   ├── 多文件队列（并发控制，max 3）
│   ├── 独立进度条 + 失败重试
│   ├── 图片处理（缩略图 WebP 300x300）
│   └── 游客上传（IP 限流，格式/大小/张数限制）
│
├── 2. 图库管理模块
│   ├── 图片列表（网格 / 列表视图切换）
│   ├── 搜索（文件名模糊搜索）
│   ├── 排序（时间 / 大小，升降序）
│   ├── 格式筛选（PNG / JPG / WebP / GIF / SVG）
│   ├── 标签筛选（标签过滤栏）
│   ├── 图片重命名（行内编辑）
│   ├── 图片标签（打标签 / 删标签 / 覆盖式保存）
│   ├── Lightbox 全屏预览（← → 切换）
│   └── 批量操作（全选 / 批量复制 / 批量删除）
│
├── 3. 链接生成模块
│   ├── URL 链接
│   ├── Markdown 格式（![alt](url)）
│   ├── HTML 格式（<img src="...">）
│   └── 一键复制 + 偏好格式记忆
│
├── 4. 回收站模块
│   ├── 软删除移入回收站
│   ├── Undo 撤销机制（30s 倒计时）
│   ├── 恢复图片
│   ├── 永久删除
│   ├── 一键清空
│   └── 自动清理（30 天过期）
│
├── 5. 账号系统模块
│   ├── 用户注册（开关控制 + IP 限流 5次/小时）
│   ├── 用户登录（JWT 7 天有效期）
│   ├── JWT 鉴权（浏览器前端）
│   ├── API Token 鉴权（外部工具）
│   ├── 游客模式（开关控制 + IP 每日限流）
│   ├── 用户信息查看
│   └── API Token 重置
│
├── 6. 管理员模块
│   ├── 用户管理（列表 / 删除用户及数据）
│   ├── 内容审核（通过 / 拒绝 / 复审面板）
│   ├── 全站统计（用户数 / 图片数 / 存储量 / 待审核）
│   └── 审核日志
│
├── 7. 统计看板模块
│   ├── 用户看板（总数 / 存储 / 月上传 / 7天趋势 SVG）
│   └── 管理员看板（全站统计）
│
└── 8. API 文档模块
    ├── /api-docs 在线文档页面
    ├── 鉴权说明（JWT / Token 切换 Tab）
    ├── 接口分组展示
    └── PicGo 接入配置指南
```

---

### 1.2 测试需求分析

| 需求编号 | 功能模块 | 测试要点 | 风险等级 |
|---------|---------|---------|---------|
| REQ-01 | 图片上传 | 单/多文件上传，文件类型校验，大小限制，进度反馈 | 🔴 高 |
| REQ-02 | 上传队列 | 并发控制（3），失败重试，完成回调刷新 | 🔴 高 |
| REQ-03 | 图库列表 | 数据隔离，分页/搜索/排序/筛选，性能（SQLite 查询） | 🔴 高 |
| REQ-04 | 图片重命名 | 行内编辑，非法字符校验，长度限制，实时更新 | 🟡 中 |
| REQ-05 | 图片标签 | 增/删/改查标签，标签过滤，特殊字符处理 | 🟡 中 |
| REQ-06 | 删除 Undo | 30s 倒计时，撤销恢复，批量删除 Undo | 🟡 中 |
| REQ-07 | 回收站 | 软删除，恢复，永久删除，30 天自动清理 | 🟡 中 |
| REQ-08 | 链接生成 | URL/MD/HTML 格式，复制反馈，偏好记忆 | 🟢 低 |
| REQ-09 | 用户注册 | 格式校验，重名检测，开关控制，IP 限流 | 🔴 高 |
| REQ-10 | 用户登录 | 凭证校验，JWT 生成，Token 过期 | 🔴 高 |
| REQ-11 | 鉴权 | JWT 中间件，API Token，游客 Token，userId 隔离 | 🔴 高 |
| REQ-12 | 游客上传 | 开关控制，IP 限流，格式/大小/张数限制 | 🟡 中 |
| REQ-13 | 图片审核 | 魔数校验，AI 评分，自动拒绝/复审/通过 | 🟡 中 |
| REQ-14 | 管理员 | 用户管理，审核操作，权限校验 | 🟡 中 |
| REQ-15 | 统计看板 | 数据准确性，SVG 图表，管理员统计 | 🟢 低 |
| REQ-16 | API 文档 | 页面可访问，信息正确，鉴权切换 | 🟢 低 |
| REQ-17 | 安全性 | XSS 防护，文件类型校验，鉴权绕过，SQL 注入 | 🔴 高 |
| REQ-18 | 性能 | 列表响应 ≤100ms（50并发），上传成功率 ≥99.5% | 🟡 中 |

---

### 1.3 测试目的

1. **功能完整性**：验证所有 v1.4 功能模块按设计文档正确实现，无遗漏、无偏差。
2. **数据安全性**：验证多用户数据隔离、鉴权机制、文件类型白名单、XSS 防护有效。
3. **系统稳定性**：验证高并发场景下系统不崩溃、数据不丢失、SQLite WAL 模式写入安全。
4. **用户体验**：验证操作流程顺畅、错误提示清晰、UI 符合赛博朋克设计规范。
5. **兼容性**：验证主流浏览器（Chrome / Edge / Firefox）下功能正常、样式正确。

---

### 1.4 测试范围

#### 1.4.1 测试范围（本次）

| 范围 | 说明 |
|------|------|
| 功能测试 | 8 大模块全部功能点 |
| 接口测试 | 24 个 API 端点 |
| 安全性测试 | XSS 注入、文件类型伪造、鉴权绕过、目录遍历 |
| 性能测试 | 图库列表、图片上传、混合场景 |
| 兼容性测试 | Chrome 120+ / Edge 120+ / Firefox 120+ |
| 易用性测试 | 操作流程、错误提示、快捷键、响应式布局 |
| 数据迁移测试 | JSON → SQLite 迁移脚本 |

#### 1.4.2 排除范围

| 排除项 | 原因 |
|------|------|
| 多主题切换 | v1.4 未实施 |
| PWA 离线支持 | v1.4 未实施 |
| Docker 环境测试 | 仅本地 Windows 测试 |
| 跨浏览器 Safari 兼容 | 暂无 macOS 环境 |
| 移动端 App 测试 | 无原生 App |
| 第三方审核 Provider 对接 | 仅测试 local 模式 |
| 生产环境性能基准 | 仅本地单机测试 |
| Nginx 反向代理集成测试 | 仅测 Express 直连 |
| CI/CD 流水线测试 | 人工执行 |

---

## 第二章 测试计划

### 2.1 测试策略

本测试采用 **分层递进** 策略，按以下顺序执行：

```
单元冒烟 → 接口测试 → 功能测试 → 集成测试 → 非功能测试
   (1h)      (2h)       (4h)       (2h)        (3h)
```

| 阶段 | 测试类型 | 执行方式 | 优先级 | 预估耗时 |
|------|---------|---------|--------|---------|
| 1. 冒烟验证 | 服务启动、基础接口可用 | 手动 + curl | P0 | 1h |
| 2. 接口测试 | 24 个 API 端点全覆盖 | curl 脚本 + Postman | P0 | 2h |
| 3. 功能测试 | 8 大模块功能用例 | 手动 + 浏览器 | P0 | 4h |
| 4. 集成测试 | 端到端业务流程 | 手动 + 场景化 | P1 | 2h |
| 5. 安全性测试 | XSS/注入/鉴权/文件 | 手动 + 脚本 | P0 | 2h |
| 6. 性能测试 | 压力 + 混合场景 | JMeter / k6 | P1 | 1h |
| 7. 兼容性测试 | 3 款浏览器 | 手动 | P2 | 1h |
| 8. 回归测试 | 缺陷修复验证 | 手动 | P1 | 1h |

**缺陷严重等级定义**：

| 等级 | 代号 | 定义 | 响应要求 |
|------|------|------|---------|
| Blocker | S0 | 系统崩溃、数据丢失、安全漏洞 | 立即修复 |
| Critical | S1 | 核心功能不可用，无替代方案 | 24h 内修复 |
| Major | S2 | 功能缺陷，有替代方案 | 本次迭代修复 |
| Minor | S3 | UI 异常、提示不准确 | 下次迭代修复 |
| Trivial | S4 | 建议优化，不影响使用 | 排期修复 |

---

### 2.2 测试环境与工具

#### 2.2.1 硬件环境

| 配置项 | 规格 |
|------|------|
| CPU | Intel Core i5+ / AMD Ryzen 5+ |
| 内存 | 16 GB+ |
| 磁盘 | 256 GB SSD+ |
| 网络 | 本地回环（localhost） |

#### 2.2.2 软件环境

| 软件 | 版本 | 用途 |
|------|------|------|
| 操作系统 | Windows 11 Pro | 测试主机 |
| Node.js | v22.21.0 | 服务运行环境 |
| npm | 10.x | 包管理 |
| Chrome | 120+ | 主测试浏览器 |
| Edge | 120+ | 副测试浏览器 |
| Firefox | 120+ | 副测试浏览器 |
| Git | 2.40+ | 版本管理 |

#### 2.2.3 测试工具

| 工具 | 版本 | 用途 |
|------|------|------|
| curl | 8.x（Git Bash 内置） | API 接口快速验证 |
| Postman | 最新版 | 接口测试集合管理、环境变量 |
| JMeter | 5.6+ | 性能压力测试 |
| 浏览器 DevTools | — | 网络监控、Console 日志、Performance 分析 |
| PowerShell | 7+ | 冒烟测试脚本执行 |
| Node.js 脚本 | — | 批量数据生成、编码测试 |
| SQLite Browser | 3.x | 数据库内容验证（可选） |

---

### 2.3 测试人员与职责分工

| 角色 | 职责 | 人员 |
|------|------|------|
| 测试负责人 | 制定测试计划、分配任务、编写报告 | hgl |
| 功能测试 | 执行功能用例、记录缺陷、回归验证 | hgl |
| 接口测试 | API 端点全覆盖、参数边界验证 | hgl |
| 安全测试 | XSS/注入/鉴权绕过/文件校验 | hgl |
| 性能测试 | JMeter 脚本编写与执行、结果分析 | hgl |

> 当前项目为个人开发，测试由开发者兼任。多人协作时建议分离开发与测试角色。

---

## 第三章 测试设计

### 3.1 功能测试用例设计

#### 3.1.1 设计方法说明

| 方法 | 适用场景 | 示例 |
|------|---------|------|
| 等价类划分 | 输入域可分组（文件名长度、文件大小） | 有效：3-100字符；无效：空、1字符、101字符 |
| 边界值分析 | 数值型约束的边界 | 文件大小：0B、1B、20MB、20MB+1B |
| 错误推测法 | 常见错误场景 | 网络断开、并发冲突、特殊字符 |
| 场景法 | 端到端业务流程 | 注册→登录→上传→复制链接→删除→Undo→恢复 |
| 正交实验 | 多因素组合 | 格式(PNG/JPG/WebP) × 大小(小/中/大) × 数量(1/5/10) |

#### 3.1.2 功能测试用例表

##### A. 上传模块（TC-UP）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-UP-001 | 单文件拖拽上传 | 已登录 | 拖拽 1 个 JPG 文件到上传区 | 上传成功，图库刷新，卡片显示缩略图 | P0 |
| TC-UP-002 | 单文件点击上传 | 已登录 | 点击上传区→选择 1 个 PNG 文件 | 上传成功，进度条显示 100% | P0 |
| TC-UP-003 | Ctrl+V 粘贴上传 | 已登录，剪贴板有图片 | 在页面上按 Ctrl+V | 上传成功 | P0 |
| TC-UP-004 | 多文件上传（10 个） | 已登录 | 拖拽 10 个 JPG 文件 | 全部上传成功，队列面板显示逐个进度 | P0 |
| TC-UP-005 | 并发控制（>3 文件） | 已登录 | 拖拽 6 个文件 | 同时最多 3 个上传，其余等待 | P1 |
| TC-UP-006 | 上传失败重试 | 已登录 | 模拟 1 个文件网络失败 | 显示重试按钮，点击后重新上传成功 | P1 |
| TC-UP-007 | 非法文件类型 | 已登录 | 上传 .exe 文件 | 返回错误 "不支持的文件类型" | P0 |
| TC-UP-008 | 超大文件 | 已登录 | 上传 21MB 文件 | Multer 拒绝，返回大小超限错误 | P0 |
| TC-UP-009 | 空文件 | 已登录 | 上传 0 字节文件 | 返回错误 "没有上传文件" | P1 |
| TC-UP-010 | 无文件上传 | 已登录 | POST /api/upload 无 files 字段 | 返回 400 "没有上传文件" | P1 |
| TC-UP-011 | 未登录上传 | 无 | 未登录状态拖拽文件 | 弹出登录弹窗 | P0 |
| TC-UP-012 | WebP 格式上传 | 已登录 | 上传 .webp 文件 | 上传成功，生成缩略图 | P1 |
| TC-UP-013 | GIF 动图上传 | 已登录 | 上传 .gif 文件 | 上传成功 | P1 |
| TC-UP-014 | SVG 矢量图上传 | 已登录 | 上传 .svg 文件 | 上传成功，无缩略图 | P2 |
| TC-UP-015 | 中文文件名上传 | 已登录 | 上传文件名含中文的图片 | 上传成功，存储名随机化 | P1 |
| TC-UP-016 | 审核自动拒绝 | 已登录，审核启用 | 上传触发高评分图片 | rejected: true，前端提示 | P1 |

##### B. 图库管理模块（TC-GL）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-GL-001 | 图片列表加载 | 已登录，有图片 | 进入首页 | 按上传时间倒序显示图片卡片 | P0 |
| TC-GL-002 | 网格/列表切换 | 已登录 | 点击 "≡ 列表" 按钮 | 切换为列表视图 | P1 |
| TC-GL-003 | 搜索过滤 | 已登录 | 在搜索框输入关键词 | 仅显示匹配图片 | P1 |
| TC-GL-004 | 排序切换 | 已登录 | 切换排序方式 | 列表按选择重新排列 | P1 |
| TC-GL-005 | 格式筛选 | 已登录 | 选择 "PNG" 筛选 | 仅显示 PNG 图片 | P1 |
| TC-GL-006 | 标签筛选 | 已登录，有标签 | 点击标签过滤栏标签 | 仅显示含该标签图片 | P1 |
| TC-GL-007 | Lightbox 预览 | 已登录 | 点击图片卡片 | 全屏预览，显示图片信息和操作按钮 | P0 |
| TC-GL-008 | Lightbox 切换 | 已登录，Lightbox 打开 | 按 ← → 键或点击按钮 | 切换上一张/下一张 | P1 |
| TC-GL-009 | 重命名-合法名称 | 已登录 | 点击重命名→输入 "测试图片-01"→回车 | 卡片文件名更新 | P0 |
| TC-GL-010 | 重命名-空名称 | 已登录 | 重命名输入为空→提交 | 提示 "文件名不能为空" | P1 |
| TC-GL-011 | 重命名-超长名称 | 已登录 | 重命名输入 101 字符 | 提示 "不能超过 100 字符" | P1 |
| TC-GL-012 | 重命名-非法字符 | 已登录 | 重命名输入含 `#%&` | 提示 "含非法字符" | P1 |
| TC-GL-013 | 重命名-Esc 取消 | 已登录 | 进入重命名→按 Esc | 恢复原文件名，取消编辑 | P2 |
| TC-GL-014 | 添加标签 | 已登录 | 点击 "+" → 输入标签 → 确认 | 标签胶囊出现，标签过滤栏更新 | P1 |
| TC-GL-015 | 删除标签 | 已登录，有标签 | 点击标签 × | 标签消失 | P1 |
| TC-GL-016 | 标签上限（10个） | 已登录 | 设置 11 个标签 | 返回 "标签最多 10 个" | P1 |
| TC-GL-017 | 标签格式非法 | 已登录 | 标签含 `#` 特殊字符 | 返回格式不合法 | P2 |
| TC-GL-018 | 复制 URL 链接 | 已登录 | 点击 URL 复制按钮 | 剪贴板含 URL，Toast 提示 | P0 |
| TC-GL-019 | 复制 Markdown | 已登录 | 点击 MD 复制按钮 | 剪贴板含 `![name](url)` | P1 |
| TC-GL-020 | 复制 HTML | 已登录 | 点击 HTML 复制按钮 | 剪贴板含 `<img src="url">` | P2 |
| TC-GL-021 | 批量选择 | 已登录 | 点击 "批量选择" → 勾选 3 张 | 工具栏显示已选数量 | P0 |
| TC-GL-022 | 全选 | 已登录，批量模式 | 点击 "全选" | 所有卡片被选中 | P1 |
| TC-GL-023 | 批量复制链接 | 已登录，批量模式，已选 3 张 | 点击 "复制链接" | 3 条链接复制到剪贴板 | P1 |
| TC-GL-024 | 批量删除 | 已登录，批量模式，已选 3 张 | 点击 "批量删除" → 确认 | 3 张移入回收站，Undo 条弹出 | P0 |
| TC-GL-025 | Esc 退出批量模式 | 已登录，批量模式 | 按 Esc | 退出批量模式，取消所有选中 | P2 |
| TC-GL-026 | 数据隔离 | 用户 A 已登录 | 用户 A 访问图库 | 仅看到自己的图片 | P0 |
| TC-GL-027 | 空状态展示 | 已登录，无图片 | 进入首页 | 显示 "// NO DATA FOUND IN VAULT" | P2 |

##### C. 回收站模块（TC-TR）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-TR-001 | 删除图片（软删除） | 已登录，有图片 | 点击删除按钮 → 确认 | 图片移入回收站，Undo 条弹出 | P0 |
| TC-TR-002 | Undo 撤销删除 | 删除后 Undo 条可见 | 30s 内点击 "撤销" | 图片恢复，图库可见 | P0 |
| TC-TR-003 | Undo 超时 | 删除后 Undo 条可见 | 等待 30 秒 | Undo 条消失，图片保留在回收站 | P1 |
| TC-TR-004 | Undo 手动关闭 | 删除后 Undo 条可见 | 点击 Undo 条 × 按钮 | Undo 条消失，图片保留在回收站 | P2 |
| TC-TR-005 | 回收站列表 | 已登录，回收站有数据 | 点击 HUD 回收站按钮 | 显示回收站图片列表（含剩余天数） | P1 |
| TC-TR-006 | 恢复图片 | 已登录，回收站有数据 | 点击 "恢复" 按钮 | 图片移回图库，回收站不显示 | P0 |
| TC-TR-007 | 永久删除 | 已登录，回收站有数据 | 点击 "永久删除" → 确认 | 图片文件和元数据彻底删除 | P0 |
| TC-TR-008 | 清空回收站 | 已登录，回收站有数据 | 点击 "清空回收站" → 确认 | 所有垃圾图片永久删除 | P1 |
| TC-TR-009 | 越权恢复 | 用户 A 已登录 | 尝试恢复用户 B 的图片 | 返回 403 | P0 |
| TC-TR-010 | 越权永久删除 | 用户 A 已登录 | 尝试永久删除用户 B 的图片 | 返回 403 | P0 |
| TC-TR-011 | 30 天自动清理 | 回收站有超过30天的数据 | 访问回收站列表 | 过期条目自动清理（文件+元数据） | P1 |

##### D. 账号系统模块（TC-AU）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-AU-001 | 正常注册 | REGISTER_ENABLED=true | 输入合法 username/password→注册 | 注册成功，返回 apiToken | P0 |
| TC-AU-002 | 用户名为空 | — | username 为空→注册 | 返回 400，提示格式错误 | P1 |
| TC-AU-003 | 用户名<3 字符 | — | username 为 "ab"→注册 | 返回 400，提示 3-20 字符 | P1 |
| TC-AU-004 | 用户名>20 字符 | — | username 21 字符→注册 | 返回 400，提示 3-20 字符 | P1 |
| TC-AU-005 | 用户名含中文 | — | username 含中文→注册 | 返回 400，仅 a-z 0-9 _ | P1 |
| TC-AU-006 | 密码<6 字符 | — | password 为 "12345"→注册 | 返回 400，提示 min 6 | P1 |
| TC-AU-007 | 重复用户名 | 用户名已存在 | 用相同 username 注册 | 返回 409 "// USERNAME TAKEN" | P0 |
| TC-AU-008 | 注册 IP 限流 | — | 1 小时内注册 6 次 | 第 6 次返回 429 | P0 |
| TC-AU-009 | 注册开关关闭 | REGISTER_ENABLED=false | 尝试注册 | 返回 403 "// REGISTRATION CLOSED" | P0 |
| TC-AU-010 | 正常登录 | 已注册账号 | 输入正确 username/password→登录 | 返回 JWT + apiToken + isAdmin | P0 |
| TC-AU-011 | 错误密码登录 | 已注册账号 | 输入错误密码→登录 | 返回 401 "WRONG CREDENTIALS" | P0 |
| TC-AU-012 | 不存在用户登录 | — | 输入不存在用户名→登录 | 返回 401 | P1 |
| TC-AU-013 | JWT 鉴权访问 | 已登录 | 携带 JWT 访问 /api/list | 正常返回数据 | P0 |
| TC-AU-014 | 无 JWT 访问 | 未登录 | 无 Authorization 头访问 /api/list | 返回 401 | P0 |
| TC-AU-015 | 过期 JWT | JWT 已过期 | 携带过期 JWT 访问 /api/list | 返回 401 "SESSION EXPIRED" | P1 |
| TC-AU-016 | 伪造 JWT | — | 伪造签名访问受保护接口 | 返回 401 | P0 |
| TC-AU-017 | API Token 鉴权 | 已登录，有 apiToken | x-upload-token 上传图片 | 上传成功 | P0 |
| TC-AU-018 | 游客模式访问 | GUEST_ENABLED=true | 未登录点击 "游客模式" | 进入游客上传界面 | P1 |
| TC-AU-019 | 游客模式关闭 | GUEST_ENABLED=false | 访问 /api/guest/upload | 返回 403 "GUEST MODE DISABLED" | P1 |
| TC-AU-020 | 查看用户信息 | 已登录 | GET /api/auth/me | 返回 username + apiToken | P1 |
| TC-AU-021 | 重置 API Token | 已登录 | POST /api/auth/reset-token | 旧 Token 失效，新 Token 可用 | P1 |
| TC-AU-022 | 管理员自动识别 | ADMIN_USERNAME 匹配 | 用 ADMIN_USERNAME 注册 | isAdmin = true | P1 |

##### E. 游客模块（TC-GS）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-GS-001 | 游客上传 JPG | GUEST_ENABLED=true | POST /api/guest/upload 上传 JPG | 上传成功，返回 guestToken | P0 |
| TC-GS-002 | 游客上传非法格式 | GUEST_ENABLED=true | 上传 .gif 文件 | 跳过（仅支持 jpg/jpeg/png/webp） | P1 |
| TC-GS-003 | 游客上传超大文件 | GUEST_ENABLED=true | 上传超过 GUEST_MAX_FILE_SIZE 的文件 | 跳过 | P0 |
| TC-GS-004 | 游客 IP 日限流 | GUEST_ENABLED=true | 超过 GUEST_DAILY_LIMIT | 返回 429 | P0 |
| TC-GS-005 | 游客删除（正确 Token） | 有 guestToken | DELETE 携带正确 guestToken | 删除成功 | P1 |
| TC-GS-006 | 游客删除（错误 Token） | — | DELETE 携带错误 guestToken | 返回 403 | P0 |
| TC-GS-007 | 游客开关关闭 | GUEST_ENABLED=false | POST /api/guest/upload | 返回 403 | P1 |

##### F. 管理员模块（TC-AD）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-AD-001 | 管理员用户列表 | 管理员已登录 | 管理员面板→用户管理 | 显示所有用户及图片统计 | P1 |
| TC-AD-002 | 管理员删除用户 | 管理员已登录 | 点击 "删除用户" → 确认 | 用户及其所有图片删除 | P0 |
| TC-AD-003 | 普通用户访问管理接口 | 普通用户已登录 | GET /api/admin/users | 返回 403 | P0 |
| TC-AD-004 | 审核通过 | 管理员已登录，有待审核 | 点击 "通过" | 图片状态变为 PASS | P1 |
| TC-AD-005 | 审核拒绝 | 管理员已登录，有待审核 | 点击 "拒绝" | 图片状态变为 REJECT，移出图库 | P1 |
| TC-AD-006 | 全站统计 | 管理员已登录 | 管理员面板→数据看板 | 显示用户数/图片数/存储量/待审核 | P1 |

##### G. 统计看板模块（TC-ST）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-ST-001 | 用户统计 | 已登录，有图片 | 打开用户面板→统计 | 显示总数/存储/月上传/趋势图 | P1 |
| TC-ST-002 | 统计准确度-总数 | 已登录，已知图片数 | 对比统计数与实际数 | 一致 | P1 |
| TC-ST-003 | 统计准确度-存储 | 已登录，已知存储量 | 对比统计存储与实际文件 | 一致（±5%） | P1 |
| TC-ST-004 | 7 天趋势图 | 已登录 | 查看 SVG 折线图 | 7 个数据点，坐标正确 | P2 |
| TC-ST-005 | 空数据统计 | 已登录，无图片 | 查看统计 | 全部为 0，趋势图全 0 | P2 |

##### H. API 文档模块（TC-DOC）

| 编号 | 用例名称 | 前置条件 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-DOC-001 | 文档页面可访问 | 服务启动 | 访问 /api-docs | 返回 200，渲染 API 文档 | P1 |
| TC-DOC-002 | 服务信息加载 | 文档页面打开 | 查看服务信息区 | 显示版本/上限/开关/格式 | P1 |
| TC-DOC-003 | 鉴权 Tab 切换 | 文档页面打开 | 切换 JWT/Token Tab | 显示对应鉴权说明 | P2 |
| TC-DOC-004 | PicGo 配置展示 | 文档页面打开 | 查看 PicGo 区域 | 显示配置 JSON 示例 | P2 |
| TC-DOC-005 | HUD API 按钮 | 已登录 | 点击 HUD "API" 按钮 | 新标签页打开 /api-docs | P2 |

---

### 3.2 非功能测试用例设计

#### 3.2.1 安全性测试用例

| 编号 | 用例名称 | 攻击类型 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|---------|--------|
| TC-SEC-001 | XSS - 文件名注入 | 存储型 XSS | 上传文件名为 `<script>alert(1)</script>.png` | 卡片显示转义后文本，不执行脚本 | P0 |
| TC-SEC-002 | XSS - 重命名注入 | 存储型 XSS | 重命名为 `<img onerror=alert(1)>` | 非法字符校验拒绝或转义显示 | P0 |
| TC-SEC-003 | XSS - 标签注入 | 存储型 XSS | 标签设为 `<script>alert(1)</script>` | 非法字符校验拒绝 | P0 |
| TC-SEC-004 | 文件类型伪造 | MIME 欺骗 | 将 .exe 改后缀为 .png 上传 | 魔数校验拒绝或审核标记 REJECT | P0 |
| TC-SEC-005 | 目录遍历 | 路径穿越 | 上传文件名含 `../../etc/passwd` | nanoid 随机重命名，原始文件名不影响路径 | P0 |
| TC-SEC-006 | 鉴权绕过-无 Token | 未授权访问 | 无 Authorization 头访问 /api/list | 返回 401 | P0 |
| TC-SEC-007 | 鉴权绕过-伪造 JWT | 未授权访问 | 携带伪造签名的 JWT | 返回 401 | P0 |
| TC-SEC-008 | 鉴权绕过-越权删除 | 水平越权 | 用户 A 删除用户 B 的图片 | 返回 403 | P0 |
| TC-SEC-009 | 鉴权绕过-越权重命名 | 水平越权 | 用户 A 重命名用户 B 的图片 | 返回 403 | P0 |
| TC-SEC-010 | 鉴权绕过-越权加标签 | 水平越权 | 用户 A 给用户 B 的图片加标签 | 返回 403 | P0 |
| TC-SEC-011 | SQL 注入-搜索 | 注入 | 搜索框输入 `' OR '1'='1` | 不返回额外数据，无报错泄露 | P0 |
| TC-SEC-012 | SQL 注入-用户名 | 注入 | 登录用户名 `admin' --` | 登录失败，无异常 | P1 |
| TC-SEC-013 | 游客越权操作 | 越权 | 游客 Token 访问需登录接口 | 返回 401/403 | P1 |
| TC-SEC-014 | 大文件 DoS | 资源耗尽 | 并发上传 10 个 20MB 文件 | 系统不崩溃，限流生效 | P1 |
| TC-SEC-015 | 请求频率攻击 | DoS | 单 IP 高频请求 /api/list | 限流返回 429 | P0 |

#### 3.2.2 性能测试用例

| 编号 | 用例名称 | 场景 | 并发数 | 预期指标 | 优先级 |
|------|---------|------|--------|---------|--------|
| TC-PERF-001 | 图库列表-50 并发 | GET /api/list | 50 | 平均响应 ≤100ms，无错误 | P1 |
| TC-PERF-002 | 图库列表-100 并发 | GET /api/list | 100 | 平均响应 ≤200ms，错误率 <1% | P2 |
| TC-PERF-003 | 图片上传-10 并发 | POST /api/upload（1MB 文件） | 10 | 成功率 ≥95% | P1 |
| TC-PERF-004 | 图片上传-30 并发 | POST /api/upload（100KB 文件） | 30 | 成功率 ≥90% | P2 |
| TC-PERF-005 | 混合场景 | 80% 列表 + 15% 上传 + 5% 删除 | 50 | 平均响应 ≤500ms | P1 |
| TC-PERF-006 | 游客上传-20 并发 | POST /api/guest/upload | 20 | 成功率 ≥90% | P2 |
| TC-PERF-007 | 持续负载 | 50 并发，持续 5 分钟 | 50 | 内存不持续增长，无 crash | P2 |
| TC-PERF-008 | SQLite 写入压力 | 连续上传 100 张图片 | 1（串行） | 无数据丢失，写入成功率 100% | P1 |

#### 3.2.3 兼容性测试用例

| 编号 | 用例名称 | 浏览器 | 测试要点 | 优先级 |
|------|---------|--------|---------|--------|
| TC-COMP-001 | Chrome 全面测试 | Chrome 120+ | 全部功能、样式、动画、快捷键 | P1 |
| TC-COMP-002 | Edge 全面测试 | Edge 120+ | 全部功能、样式、快捷键 | P1 |
| TC-COMP-003 | Firefox 全面测试 | Firefox 120+ | 全部功能、样式（CSS 变量兼容） | P1 |
| TC-COMP-004 | 响应式布局-平板 | Chrome 1024px 宽度 | 布局不错乱，卡片自适应 | P2 |
| TC-COMP-005 | 响应式布局-小屏 | Chrome 375px 宽度 | 移动端基本可用 | P2 |
| TC-COMP-006 | prefers-reduced-motion | Chrome + 系统设置 | 动画减弱，Undo 条等动效停用 | P2 |

#### 3.2.4 易用性测试用例

| 编号 | 用例名称 | 测试要点 | 预期结果 | 优先级 |
|------|---------|---------|---------|--------|
| TC-UX-001 | 拖拽上传反馈 | 拖拽时 dropzone 高亮 | 拖入时边框发光，离开时恢复 | P1 |
| TC-UX-002 | 粘贴上传提示 | Ctrl+V 时 Toast 提示 | 显示上传中/成功/失败反馈 | P1 |
| TC-UX-003 | 错误提示可理解 | 各场景错误消息 | 用户能理解错误原因（非技术术语） | P1 |
| TC-UX-004 | 操作确认 | 删除/清空操作 | 弹出确认对话框，防止误操作 | P1 |
| TC-UX-005 | 快捷键可用性 | `?` 帮助面板 | 列出所有快捷键，说明清晰 | P2 |
| TC-UX-006 | 首次使用引导 | 新用户首次访问 | 登录/注册流程清晰 | P2 |
| TC-UX-007 | 赛博朋克风格一致性 | 全部 UI 元素 | 使用 `:root` 变量，无硬编码颜色 | P1 |
| TC-UX-008 | 加载状态反馈 | 网络请求期间 | 按钮禁用 + loading 状态 | P2 |

---

### 3.3 测试数据和脚本准备

#### 3.3.1 功能测试数据

**测试图片素材**：

| 文件名 | 格式 | 大小 | 尺寸 | 用途 |
|------|------|------|------|------|
| test-valid.jpg | JPG | 500KB | 1920×1080 | 正常上传 |
| test-valid.png | PNG | 200KB | 800×600 | 透明底上传 |
| test-valid.webp | WebP | 150KB | 1024×768 | WebP 格式上传 |
| test-valid.gif | GIF | 300KB | 400×300 | GIF 动图上传 |
| test-valid.svg | SVG | 5KB | vector | SVG 矢量上传 |
| test-chinese-你好.png | PNG | 100KB | 100×100 | 中文文件名 |
| test-oversize.jpg | JPG | 21MB | 4000×3000 | 超大文件拒绝 |
| test-empty.jpg | JPG | 0B | — | 空文件拒绝 |
| test-fake-png.exe | EXE（伪装 PNG） | 100KB | — | 魔数校验 |
| test-xss-name.png | PNG | 10KB | 10×10 | XSS 文件名 |

**测试账号**：

| 角色 | 用户名 | 密码 | 用途 |
|------|------|------|------|
| 管理员 | hgl（同步 .env ADMIN_USERNAME） | admin123 | 管理功能测试 |
| 普通用户 A | testuser_a | test123456 | 功能测试、数据隔离 |
| 普通用户 B | testuser_b | test123456 | 越权测试 |
| 游客 | — | — | 游客功能测试 |

#### 3.3.2 接口测试脚本（curl）

```bash
#!/bin/bash
# NEON.IMG API 接口冒烟测试
BASE="http://localhost:3000"

echo "=== 1. 服务信息 ==="
curl -s $BASE/api/info | head -c 100
echo ""

echo "=== 2. 注册用户 ==="
curl -s -X POST $BASE/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"tester_api","password":"test123"}'
echo ""

echo "=== 3. 登录 ==="
LOGIN=$(curl -s -X POST $BASE/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"tester_api","password":"test123"}')
echo "$LOGIN"
JWT=$(echo "$LOGIN" | node -e "process.stdin.on('data',d=>{try{console.log(JSON.parse(d).data.jwt)}catch(e){console.log('')}})")
echo "JWT=$JWT"

echo "=== 4. 获取用户信息 ==="
curl -s $BASE/api/auth/me -H "Authorization: Bearer $JWT"
echo ""

echo "=== 5. 上传图片 ==="
# 创建测试图片
node -e "require('fs').writeFileSync('/tmp/test_upload.png',Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYPj/HwADBwIAMCbHYQAAAABJRU5ErkJggg==','base64'))"
RES=$(curl -s -X POST $BASE/api/upload \
  -H "Authorization: Bearer $JWT" \
  -F "files=@/tmp/test_upload.png")
echo "$RES"
IMG_ID=$(echo "$RES" | node -e "process.stdin.on('data',d=>{try{console.log(JSON.parse(d).data[0].id)}catch(e){console.log('')}})")
echo "IMG_ID=$IMG_ID"

echo "=== 6. 图片列表 ==="
curl -s $BASE/api/list -H "Authorization: Bearer $JWT" | head -c 200
echo ""

echo "=== 7. 重命名 ==="
curl -s -X PATCH "$BASE/api/image/$IMG_ID/rename" \
  -H "Authorization: Bearer $JWT" \
  -H "Content-Type: application/json" \
  -d '{"name":"renamed-by-api.png"}'
echo ""

echo "=== 8. 设置标签 ==="
curl -s -X POST "$BASE/api/image/$IMG_ID/tags" \
  -H "Authorization: Bearer $JWT" \
  -H "Content-Type: application/json" \
  -d '{"tags":["test","api"]}'
echo ""

echo "=== 9. 获取标签 ==="
curl -s $BASE/api/tags -H "Authorization: Bearer $JWT"
echo ""

echo "=== 10. 用户统计 ==="
curl -s $BASE/api/stats/me -H "Authorization: Bearer $JWT"
echo ""

echo "=== 11. 删除图片 ==="
curl -s -X DELETE "$BASE/api/image/$IMG_ID" \
  -H "Authorization: Bearer $JWT"
echo ""

echo "=== 12. 回收站列表 ==="
curl -s $BASE/api/trash -H "Authorization: Bearer $JWT" | head -c 200
echo ""

echo "=== 13. 恢复图片 ==="
curl -s -X POST "$BASE/api/restore/$IMG_ID" \
  -H "Authorization: Bearer $JWT"
echo ""

echo "=== 14. 永久删除 ==="
# 先删除再永久删除
curl -s -X DELETE "$BASE/api/image/$IMG_ID" -H "Authorization: Bearer $JWT" > /dev/null
curl -s -X DELETE "$BASE/api/purge/$IMG_ID" -H "Authorization: Bearer $JWT"
echo ""

echo "=== 15. API 文档页面 ==="
curl -s -o /dev/null -w "HTTP %{http_code}" $BASE/api-docs
echo ""

echo "=== 16. 鉴权测试（无 Token） ==="
curl -s $BASE/api/list
echo ""

echo "=== 17. 越权限测试 ==="
# 切换到 tester_b
LOGIN_B=$(curl -s -X POST $BASE/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"tester_b","password":"test123"}' 2>/dev/null)
# 上传 tester_a 的图片
# 用 tester_b 的 JWT 删除 tester_a 的图片 — 应返回 403
echo "（略）"

echo ""
echo "=== 接口冒烟测试完成 ==="
```

#### 3.3.3 性能测试脚本（JMeter）

**JMeter 测试计划结构**：

```
NEON.IMG 性能测试计划
├── 测试片段（可复用）
│   ├── 用户登录（获取 JWT）
│   └── 图片上传（预生成测试文件）
├── 线程组 1：图库列表压测
│   ├── HTTP 请求：GET /api/list
│   ├── HTTP Header Manager：Authorization: Bearer ${JWT}
│   └── 聚合报告监听器
├── 线程组 2：图片上传压测
│   ├── HTTP 请求：POST /api/upload
│   ├── HTTP Header Manager：Authorization: Bearer ${JWT}
│   └── 聚合报告监听器
└── 线程组 3：混合场景
    ├── 80% Throughput Controller → 列表请求
    ├── 15% Throughput Controller → 上传请求
    ├── 5% Throughput Controller → 删除请求
    └── 聚合报告监听器
```

**关键 JMeter 配置**：

| 参数 | 值 | 说明 |
|------|------|------|
| Thread Group → Ramp-Up | 10s | 逐步加压，避免瞬时尖峰 |
| Thread Group → Duration | 300s | 持续 5 分钟 |
| HTTP Request → Timeout | 10000ms | 超时 10 秒 |
| CSV Data Set → 测试文件列表 | test_files.csv | 不同大小的上传文件 |

---

## 第四章 测试执行

### 4.1 功能测试执行

> 执行说明：每个用例执行后标注 ✅ 通过 / ❌ 失败 / ⚠️ 阻塞，失败用例填写缺陷 ID。

#### 4.1.1 图片上传模块功能测试

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-UP-001 | 单文件拖拽上传 | | |
| TC-UP-002 | 单文件点击上传 | | |
| TC-UP-003 | Ctrl+V 粘贴上传 | | |
| TC-UP-004 | 多文件上传（10 个） | | |
| TC-UP-005 | 并发控制（>3 文件） | | |
| TC-UP-006 | 上传失败重试 | | |
| TC-UP-007 | 非法文件类型 | | |
| TC-UP-008 | 超大文件 | | |
| TC-UP-009 | 空文件 | | |
| TC-UP-010 | 无文件上传 | | |
| TC-UP-011 | 未登录上传 | | |
| TC-UP-012 | WebP 格式上传 | | |
| TC-UP-013 | GIF 动图上传 | | |
| TC-UP-014 | SVG 矢量图上传 | | |
| TC-UP-015 | 中文文件名上传 | | |
| TC-UP-016 | 审核自动拒绝 | | |

#### 4.1.2 图库管理模块功能测试

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-GL-001 | 图片列表加载 | | |
| TC-GL-002 | 网格/列表切换 | | |
| TC-GL-003 | 搜索过滤 | | |
| TC-GL-004 | 排序切换 | | |
| TC-GL-005 | 格式筛选 | | |
| TC-GL-006 | 标签筛选 | | |
| TC-GL-007 | Lightbox 预览 | | |
| TC-GL-008 | Lightbox 切换 | | |
| TC-GL-009 | 重命名-合法名称 | | |
| TC-GL-010 | 重命名-空名称 | | |
| TC-GL-011 | 重命名-超长名称 | | |
| TC-GL-012 | 重命名-非法字符 | | |
| TC-GL-013 | 重命名-Esc 取消 | | |
| TC-GL-014 | 添加标签 | | |
| TC-GL-015 | 删除标签 | | |
| TC-GL-016 | 标签上限（10个） | | |
| TC-GL-017 | 标签格式非法 | | |
| TC-GL-018 | 复制 URL 链接 | | |
| TC-GL-019 | 复制 Markdown | | |
| TC-GL-020 | 复制 HTML | | |
| TC-GL-021 | 批量选择 | | |
| TC-GL-022 | 全选 | | |
| TC-GL-023 | 批量复制链接 | | |
| TC-GL-024 | 批量删除 | | |
| TC-GL-025 | Esc 退出批量模式 | | |
| TC-GL-026 | 数据隔离 | | |
| TC-GL-027 | 空状态展示 | | |

#### 4.1.3 链接生成模块功能测试

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-GL-018 | 复制 URL 链接 | | |
| TC-GL-019 | 复制 Markdown | | |
| TC-GL-020 | 复制 HTML | | |
| TC-GL-023 | 批量复制链接 | | |

> 链接生成功能已整合在图库管理模块中（TC-GL-018~020），此处为独立章节便于追踪。

#### 4.1.4 回收站模块功能测试

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-TR-001 | 删除图片（软删除） | | |
| TC-TR-002 | Undo 撤销删除 | | |
| TC-TR-003 | Undo 超时 | | |
| TC-TR-004 | Undo 手动关闭 | | |
| TC-TR-005 | 回收站列表 | | |
| TC-TR-006 | 恢复图片 | | |
| TC-TR-007 | 永久删除 | | |
| TC-TR-008 | 清空回收站 | | |
| TC-TR-009 | 越权恢复 | | |
| TC-TR-010 | 越权永久删除 | | |
| TC-TR-011 | 30 天自动清理 | | |

#### 4.1.5 账号系统模块功能测试

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-AU-001 | 正常注册 | | |
| TC-AU-002 | 用户名为空 | | |
| TC-AU-003 | 用户名<3 字符 | | |
| TC-AU-004 | 用户名>20 字符 | | |
| TC-AU-005 | 用户名含中文 | | |
| TC-AU-006 | 密码<6 字符 | | |
| TC-AU-007 | 重复用户名 | | |
| TC-AU-008 | 注册 IP 限流 | | |
| TC-AU-009 | 注册开关关闭 | | |
| TC-AU-010 | 正常登录 | | |
| TC-AU-011 | 错误密码登录 | | |
| TC-AU-012 | 不存在用户登录 | | |
| TC-AU-013 | JWT 鉴权访问 | | |
| TC-AU-014 | 无 JWT 访问 | | |
| TC-AU-015 | 过期 JWT | | |
| TC-AU-016 | 伪造 JWT | | |
| TC-AU-017 | API Token 鉴权 | | |
| TC-AU-018 | 游客模式访问 | | |
| TC-AU-019 | 游客模式关闭 | | |
| TC-AU-020 | 查看用户信息 | | |
| TC-AU-021 | 重置 API Token | | |
| TC-AU-022 | 管理员自动识别 | | |

#### 4.1.6 管理员模块功能测试

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-AD-001 | 管理员用户列表 | | |
| TC-AD-002 | 管理员删除用户 | | |
| TC-AD-003 | 普通用户访问管理接口 | | |
| TC-AD-004 | 审核通过 | | |
| TC-AD-005 | 审核拒绝 | | |
| TC-AD-006 | 全站统计 | | |

---

### 4.2 性能测试执行

> **前置：获取测试 JWT**（JMeter / k6 压测需要预先准备有效的 JWT）

```bash
# 注册测试账号（仅首次）
curl -s -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"perf_test","password":"test123456"}'

# 登录获取 JWT（7 天有效，压测前执行一次即可）
curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"perf_test","password":"test123456"}'
# → 复制 data.jwt 的值，填入 JMeter 的 Authorization Header

# 一键提取 JWT 到环境变量
JWT=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"perf_test","password":"test123456"}' \
  | node -e "process.stdin.on('data',d=>console.log(JSON.parse(d).data.jwt))")
echo "JWT=$JWT"

# 验证 JWT 可用
curl -s http://localhost:3000/api/list -H "Authorization: Bearer $JWT"
```

#### 4.2.1 单场景压力测试（图库列表接口）

**测试配置**：

| 参数 | 值 |
|------|------|
| 接口 | GET /api/list |
| 鉴权 | Bearer JWT（预登录获取） |
| 并发数 | 50 / 100 |
| 持续时间 | 60s |
| 预热时间 | 10s |
| 当前数据量 | ~30 张图片（SQLite） |

**测试结果**：

| 指标 | 50 并发 | 100 并发 | 判定 |
|------|--------|---------|------|
| 平均响应时间 | | | |
| P95 响应时间 | | | |
| P99 响应时间 | | | |
| 吞吐量（req/s） | | | |
| 错误率 | | | |
| 内存峰值 | | | |

#### 4.2.2 单场景压力测试（图片上传接口）

**测试配置**：

| 参数 | 值 |
|------|------|
| 接口 | POST /api/upload |
| 上传文件 | 1MB JPEG |
| 并发数 | 10 |
| 持续时间 | 60s |

**测试结果**：

| 指标 | 结果 | 判定 |
|------|------|------|
| 平均响应时间 | | |
| 上传成功率 | | |
| 数据完整率 | | |
| 磁盘写入量 | | |

#### 4.2.3 混合场景压力测试

**测试配置**：

| 操作 | 比例 | 说明 |
|------|------|------|
| GET /api/list | 80% | 图库列表查询 |
| POST /api/upload | 15% | 图片上传（100KB） |
| DELETE /api/image/:id | 5% | 删除图片 |

**并发**：50，持续 5 分钟

**测试结果**：

| 指标 | 结果 | 判定 |
|------|------|------|
| 平均响应时间 | | |
| 错误率 | | |
| 内存趋势 | | |
| 数据一致性 | | |

---

### 4.3 安全测试执行

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-SEC-001 | XSS - 文件名注入 | | |
| TC-SEC-002 | XSS - 重命名注入 | | |
| TC-SEC-003 | XSS - 标签注入 | | |
| TC-SEC-004 | 文件类型伪造 | | |
| TC-SEC-005 | 目录遍历 | | |
| TC-SEC-006 | 鉴权绕过-无 Token | | |
| TC-SEC-007 | 鉴权绕过-伪造 JWT | | |
| TC-SEC-008 | 鉴权绕过-越权删除 | | |
| TC-SEC-009 | 鉴权绕过-越权重命名 | | |
| TC-SEC-010 | 鉴权绕过-越权加标签 | | |
| TC-SEC-011 | SQL 注入-搜索 | | |
| TC-SEC-012 | SQL 注入-用户名 | | |
| TC-SEC-013 | 游客越权操作 | | |
| TC-SEC-014 | 大文件 DoS | | |
| TC-SEC-015 | 请求频率攻击 | | |

---

### 4.4 兼容性测试执行

| 编号 | 浏览器 | 版本 | 结果 | 备注 |
|------|--------|------|------|------|
| TC-COMP-001 | Chrome | | | |
| TC-COMP-002 | Edge | | | |
| TC-COMP-003 | Firefox | | | |
| TC-COMP-004 | Chrome 1024px | | | |
| TC-COMP-005 | Chrome 375px | | | |
| TC-COMP-006 | prefers-reduced-motion | | | |

---

### 4.5 易用性测试执行

| 编号 | 用例名称 | 结果 | 备注 |
|------|---------|------|------|
| TC-UX-001 | 拖拽上传反馈 | | |
| TC-UX-002 | 粘贴上传提示 | | |
| TC-UX-003 | 错误提示可理解 | | |
| TC-UX-004 | 操作确认 | | |
| TC-UX-005 | 快捷键可用性 | | |
| TC-UX-006 | 首次使用引导 | | |
| TC-UX-007 | 赛博朋克风格一致性 | | |
| TC-UX-008 | 加载状态反馈 | | |

---

## 第五章 测试总结（模板）

### 5.1 测试统计

| 统计项 | 数值 |
|------|------|
| 测试用例总数 | 111 |
| 已执行 | |
| 通过 | |
| 失败 | |
| 阻塞 | |
| 通过率 | |
| 发现缺陷总数 | |
| S0（Blocker） | |
| S1（Critical） | |
| S2（Major） | |
| S3（Minor） | |
| S4（Trivial） | |

### 5.2 缺陷分布

| 模块 | 缺陷数 | 占比 |
|------|--------|------|
| 上传模块 | | |
| 图库管理模块 | | |
| 回收站模块 | | |
| 账号系统模块 | | |
| 游客模块 | | |
| 管理员模块 | | |
| 统计看板 | | |
| API 文档 | | |

### 5.3 测试结论

> [ 测试完成后填写 ]

- **功能完整性**：
- **安全性**：
- **性能表现**：
- **兼容性**：
- **遗留风险**：
- **建议**：

---

*— END OF TEST DOCUMENT // NEON.IMG v1.4 —*
