# NEON.IMG v1.3 优化方案

> 版本号：v1.3  
> 文档日期：2026-05-27  
> 维护者：hgl  
> 适用范围：仅描述 v1.3 迭代中的优化方案，不包含现状回顾与历史版本内容

---

## 一、版本目标

v1.3 聚焦四条主线：

1. **更大容量**：单文件上限由 10MB 提升至 20MB，并打通配套的上传链路优化。
2. **更低门槛**：开放游客匿名上传通道，扩大使用场景。
3. **更高合规**：引入图片审核机制，满足内容安全与合规要求。
4. **更易落地**：提供完整的自托管部署文档与一键部署脚本。

---

## 二、单文件上限提升至 20MB

### 2.1 前端调整
- 上传区文案更新：`MAX 10MB` → `MAX 20MB / FILE`，并补充总队列上限说明（建议 100MB / 次）。
- 文件选择后立即在前端做大小校验，超限文件标记为 `OVERSIZE` 并禁止入队。
- 大文件预览使用 `URL.createObjectURL` 异步生成缩略图，避免阻塞主线程。

### 2.2 上传链路
- 启用 **分片上传**（建议 4MB / 片）+ 断点续传，针对 ≥ 8MB 的文件自动切换分片模式。
- 失败分片自动重试 3 次，超过则提示并保留已上传分片 24 小时。
- 上传并发数限制为 3，避免移动端弱网下整体失败。

### 2.3 服务端调整
- Nginx / 反向代理层：`client_max_body_size` 调整为 25MB（预留 buffer）。
- 应用层：增加 `MAX_FILE_SIZE=20MB` 与 `MAX_BATCH_SIZE=100MB` 两个环境变量。
- 对象存储分片合并使用服务端 Multipart Upload，减少应用服务器内存压力。

### 2.4 体验配套
- 上传进度条按"文件级 + 分片级"两层展示。
- 大文件上传完成后给出"是否自动压缩为 WebP 以节省空间"的可选提示。

---

## 三、游客匿名上传通道

### 3.1 通道设计
- 在登录入口旁新增 `>> GUEST_MODE` 入口，无需注册即可上传。
- 游客上传走独立路径前缀（如 `/g/`），便于与登录用户资源隔离。
- 游客上传成功后返回：
  - 永久外链 URL（受保留策略影响）
  - 一次性"管理令牌"（用于在保留期内删除该图）

### 3.2 限制策略
| 维度         | 登录用户           | 游客                               |
| ------------ | ------------------ | ---------------------------------- |
| 单文件上限   | 20MB               | 10MB                               |
| 每日上传次数 | 不限（按账户额度） | 20 次 / IP / 日                    |
| 单次队列上限 | 10 张              | 3 张                               |
| 文件保留期   | 永久               | 30 天                              |
| 支持格式     | 全部               | PNG / JPG / WEBP（禁用 SVG / GIF） |

> 禁用 SVG 是为了避免匿名通道下 XSS 风险；禁用 GIF 主要出于带宽考虑，可按运营情况调整。

### 3.3 风控
- IP + 浏览器指纹双维度限流。
- 引入轻量级人机验证（Turnstile / hCaptcha），仅在异常流量时弹出。
- 游客通道默认开启更严格的审核策略（详见第四节）。

### 3.4 隐私
- 游客上传不记录登录态，但保留必要的审计日志（哈希后的 IP、UA、时间、文件指纹）。
- 隐私声明在游客入口处明确提示，需勾选同意后方可上传。

---

## 四、图片审核（合规）

### 4.1 审核架构
- 采用 **三级管线**：本地预检 → 云端 AI 审核 → 人工复审。
- 所有上传统一进入审核队列，状态机：`PENDING → PASS / REJECT / NEED_REVIEW`。
- 审核未通过的文件转入隔离桶，外链返回 `451 Unavailable For Legal Reasons` 占位图。

### 4.2 本地预检
- 文件魔数校验，拒绝伪装的可执行文件。
- 哈希比对：对接已知违规图库（PhotoDNA 风格的感知哈希）秒级命中拦截。
- EXIF 清理（默认开启），降低隐私泄露。

### 4.3 云端 AI 审核
- 接入可插拔的审核服务接口（抽象 `ContentModerator` 接口，便于切换供应商）。
- 审核维度建议覆盖：
  - 涉政 / 暴恐 / 色情 / 血腥
  - 广告 / 二维码 / 联系方式
  - 版权水印识别（可选）
- 审核结果带置信度，配置阈值：
  - `score ≥ 0.9` → 自动拒绝
  - `0.6 ≤ score < 0.9` → 进入人工复审
  - `score < 0.6` → 通过

### 4.4 人工复审后台
- 独立管理后台 `/admin/moderation`，仅管理员可见。
- 列表展示：缩略图、上传者（或游客 IP 哈希）、AI 评分、命中标签。
- 操作：通过 / 拒绝 / 拉黑用户 / 拉黑指纹。
- 操作全部留痕，支持导出审计报告。

### 4.5 用户侧反馈
- 上传后若进入审核中：图库占位显示 `>> UNDER_REVIEW`，外链暂不可用。
- 被拒绝：明确给出拒绝原因大类（不暴露具体规则细节），并提供申诉入口。
- 申诉流程：用户提交说明 → 管理员复审 → 邮件 / 站内通知结果。

### 4.6 合规配套
- 后台支持配置敏感词与黑名单标签，便于本地化合规调整。
- 日志保留期默认 180 天，支持按法规要求延长。
- 数据出境合规：审核服务支持选择"仅境内供应商"模式。

---

## 五、自托管部署文档与脚本

### 5.1 文档结构
建议在仓库新增 `docs/self-hosting/` 目录，包含：

- `README.md`：5 分钟快速开始
- `architecture.md`：系统架构与依赖说明
- `requirements.md`：服务器配置、域名、证书要求
- `install-docker.md`：Docker Compose 部署（推荐）
- `install-k8s.md`：Kubernetes Helm Chart 部署
- `install-bare-metal.md`：裸机 / systemd 部署
- `configuration.md`：环境变量完整说明
- `storage-backends.md`：S3 / R2 / OSS / MinIO 配置示例
- `moderation-setup.md`：审核服务对接指南
- `upgrade.md`：版本升级与数据迁移
- `backup-restore.md`：备份与恢复
- `troubleshooting.md`：常见问题与日志排查

### 5.2 一键部署
- 提供官方 `docker-compose.yml`，开箱即用，组件包括：
  - `neon-img-web`（前端）
  - `neon-img-api`（后端）
  - `neon-img-worker`（审核 / 转码任务）
  - `postgres`（元数据）
  - `redis`（队列与限流）
  - `minio`（默认对象存储，可替换）
- 提供 `install.sh` 引导脚本：交互式收集域名、管理员邮箱、存储后端、审核服务密钥，并自动生成 `.env`。

### 5.3 配置规范
- 所有可调参数集中到 `.env.example`，关键项包括：
  - `MAX_FILE_SIZE` / `MAX_BATCH_SIZE`
  - `GUEST_ENABLED` / `GUEST_DAILY_LIMIT` / `GUEST_RETENTION_DAYS`
  - `MODERATION_PROVIDER` / `MODERATION_THRESHOLD_REJECT` / `MODERATION_THRESHOLD_REVIEW`
  - `STORAGE_DRIVER` / `STORAGE_BUCKET` / `STORAGE_REGION`
  - `SMTP_*`（用于审核通知与申诉）
- 配置变更后通过 `neon-img reload` 命令热更新，避免重启。

### 5.4 运维支持
- 内置健康检查：`/healthz`（存活）、`/readyz`（就绪）。
- 默认提供 Prometheus 指标端点 `/metrics`，附带 Grafana Dashboard JSON 模板。
- 默认日志输出为 JSON，便于接入 Loki / ELK。
- 提供 `neon-img doctor` 自检命令，自动检查数据库连接、存储桶可写性、审核服务连通性等。

### 5.5 升级与回滚
- 数据库迁移使用版本化脚本，升级命令：`neon-img migrate up`。
- 镜像 Tag 同时维护 `latest` 与 `vX.Y.Z`，强烈建议生产环境锁定语义化版本。
- 升级前置检查脚本：磁盘空间、备份完整性、外部依赖可达性。

---

## 六、辅助优化（围绕四条主线的必要补充）

为保证上述四项主线落地稳定，本版本同时合入以下小范围优化：

- **上传体验**：失败重试、Toast 反馈、Undo 删除（30s 内可撤销），降低误操作。
- **图库标记**：图片卡片新增 `GUEST` / `REVIEWING` / `REJECTED` 状态徽章，提升可识别性。
- **管理后台雏形**：仅上线审核相关模块，其他管理功能延后至 v1.4。
- **安全加固**：上传接口加入签名校验，防止外部脚本绕过前端直接打接口。
- **文档同步**：更新 API 文档中与上限、游客通道、审核状态相关的字段与错误码。

---

## 七、里程碑（建议节奏）

| 阶段        | 周期         | 关键产出                             |
| ----------- | ------------ | ------------------------------------ |
| 设计冻结    | 第 1 周      | 接口定义、审核管线评审、文档大纲     |
| 开发主线    | 第 2 \~ 4 周 | 20MB 分片上传、游客通道、审核接入    |
| 自托管文档  | 第 3 \~ 4 周 | docker-compose、install.sh、文档站点 |
| 联调 & 灰度 | 第 5 周      | 内部小流量验证、审核阈值调参         |
| 全量发布    | 第 6 周初    | v1.3 GA、发布说明、升级指南          |

---

> END_OF_FILE //  ◤ NEON.IMG v1.3 ◢  · Powered by Night City