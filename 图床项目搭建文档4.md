# NEON.IMG v1.4 优化方案

> **文档版本**：v1.4
> **撰写日期**：2026-05-28
> **更新日期**：2026-05-29（移除 4 个未实施项，整理为 v1.4 实际交付内容）
> **维护者**：hgl
> **适用范围**：仅描述 v1.4 迭代中的优化方向，不包含历史版本内容

---

## 一、版本目标

v1.4 聚焦三条主线：

1. **更好用**：图片重命名、上传队列管理、Undo 撤销，覆盖日常高频操作痛点。
2. **更强大**：标签分类、数据看板、开放 API，让产品从工具走向平台。
3. **更稳定**：SQLite 替换 JSON 存储，提升工程质量。

---

## 二、优化方向总览

| 优先级 | 方向 | 类型 | 状态 |
|-------|------|------|------|
| 🔴 P0 | 图片重命名 | 体验 | ✅ 已完成 |
| 🔴 P0 | 上传队列管理升级 | 体验 | ✅ 已完成 |
| 🔴 P0 | 删除 Undo 撤销机制 | 体验 | ✅ 已完成 |
| 🟡 P1 | 图片标签与分类 | 功能 | ✅ 已完成 |
| 🟡 P1 | 使用统计与数据看板 | 功能 | ✅ 已完成 |
| 🟡 P1 | 开放 API 文档（PicGo 接入） | 功能 | ✅ 已完成 |
| 🟢 P2 | SQLite 数据库迁移 | 工程 | ⏳ 待确认（需引入 better-sqlite3） |

---

## 三、P0 高优方向（体验类）

### 3.1 图片重命名

**背景**：上传图片后文件名往往是截图工具自动生成的无意义字符串，用户无法直接在图库内修改，需要重新下载后上传。

**功能设计**：

- 卡片底部操作区新增重命名按钮（✎ 图标），位于文件大小和删除按钮之间
- 点击后卡片底部切换为行内编辑状态，无需弹窗
- 支持中文、字母、数字、空格、连字符、下划线、点，最长 100 字符
- 按 Enter 或点击 ✓ 提交，按 Esc 或点击 ✕ 取消
- 提交成功后卡片文件名实时更新，无需刷新页面

**行内编辑交互示意**：

```
正常状态：
  2.3 MB   [ ✎ ]  [ 删除 ]

编辑状态：
  [ screenshot-2026___________ ]  [ ✓ ]  [ ✕ ]
         ↑ input 自动聚焦，全选当前文件名
```

**后端接口**：

```
PATCH /api/image/:id/rename
Authorization: Bearer <jwt>
Body: { "name": "新文件名" }

校验规则：
  - 不能为空
  - 长度 1~100 字符
  - 正则：/^[一-龥a-zA-Z0-9 _\-\.]{1,100}$/

成功返回：{ code: 0, data: { id, name } }
失败返回：{ code: 1, msg: '// 文件名格式不合法' }
```

**注意**：重命名只更新 `images.json` 中的 `name` 元数据字段，**不修改** `uploads/` 目录下的实际文件名。

---

### 3.2 上传队列管理升级

**背景**：多文件上传时，进度条只显示整体进度，无法看到单个文件的状态，失败了也无法单独重试。

**功能设计**：

- 上传区下方展开队列面板，每个文件单独一行显示：
  - 文件名（截断）+ 文件大小
  - 独立进度条（霓虹青填充）
  - 状态标识：`上传中` / `✓ 完成` / `✗ 失败` / `等待中`
- 单文件失败时显示 `[ 重试 ]` 按钮，不影响其他文件
- 全部完成后队列面板 3 秒后自动收起
- 上传并发数限制为 3，避免弱网环境整体失败

**队列面板示意**：

```
// 上传队列 //
[████████░░] 80%  screenshot.png    4.2 MB   上传中
[██████████] 100% avatar.jpg        1.1 MB   ✓ 完成
[░░░░░░░░░░] 0%   banner.webp       2.8 MB   等待中
```

---

### 3.3 删除 Undo 撤销机制

**背景**：误删图片后只能去回收站恢复，操作路径较长，体验不够顺滑。

**功能设计**：

- 点击删除确认后，在页面底部弹出 Undo 提示条（30 秒倒计时）：
  ```
  // 数据包已移入回收站 // [ 撤销 ] [ × ]  (29s)
  ```
- 30 秒内点击 `[ 撤销 ]` 直接恢复，无需进入回收站
- 30 秒后自动消失，删除操作正式生效
- 批量删除同样支持 Undo，一次性恢复全部

**技术方案**：
- Undo 提示条固定在页面底部，JS 实现 30 秒倒计时
- 后端软删除逻辑不变，Undo 调用已有 `/api/restore/:id` 接口

---

## 四、P1 功能扩展方向（已完成）

### 4.1 图片标签与分类

**功能设计**：

| 功能 | 说明 |
|------|------|
| 打标签 | 每张图片可添加多个 Tag，覆盖式保存 |
| 标签筛选 | 图库顶部标签过滤栏，点击快速过滤 |
| 标签管理 | 卡片上展示标签胶囊，支持增删 |

**后端接口**：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/tags` | 获取当前用户所有标签（去重汇总） |
| POST | `/api/image/:id/tags` | 覆盖式设置标签（最多 10 个） |
| DELETE | `/api/image/:id/tags/:tag` | 删除图片单个标签 |

**数据结构**（images.json 扩展）：

```json
{
  "tags": ["截图", "UI设计", "参考图"]
}
```

**UI 风格**：
- 标签样式：霓虹色小胶囊（`.card-tag`），背景 `--neon-cyan` 半透明
- 标签过滤栏：图库控件区下方，横向排列常用标签

---

### 4.2 使用统计与数据看板

**用户侧看板**（点击 HUD 用户名进入用户面板）：

| 指标 | 说明 |
|------|------|
| 总图片数 | 当前图库图片总量 |
| 总存储量 | 所有图片占用磁盘空间 |
| 本月上传数 | 当月新增图片数量 |
| 回收站数量 | 当前回收站中图片数 |
| 最近 7 天上传趋势 | 纯 SVG 折线图 |

**管理员侧看板**（管理员面板新增 `[ 数据看板 ]` Tab）：

| 指标 | 说明 |
|------|------|
| 全站用户数 | 注册用户总量 |
| 全站图片数 | 所有用户图片总量 |
| 全站存储量 | 磁盘占用统计 |
| 今日上传数 | 当天新增图片 |
| 游客上传数 | 游客通道上传量 |
| 待审核数 | NEED_REVIEW 状态数量 |

**后端接口**：

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/stats/me` | JWT | 当前用户统计数据 |
| GET | `/api/admin/stats` | Admin | 全站统计数据 |

**技术方案**：
- 统计数据实时计算（遍历 images.json / users.json），不引入独立统计数据库
- 图表用内联 SVG 手写折线图，保持零依赖

---

### 4.3 开放 API 文档（PicGo 接入）

**API 文档页面**（`/api-docs` 路由）：
- 展示所有开放接口的调用方式、参数和返回格式
- 赛博朋克风格文档页面，复用主站 `style.css`
- 包含鉴权说明（Bearer JWT / x-upload-token 两种方式），可切换 Tab 查看
- 服务信息区动态获取 `/api/info` 展示当前配置

**PicGo 接入配置示例**：

```json
{
  "picBed": {
    "type": "web",
    "webUrl": "https://你的域名/api/upload",
    "headers": {
      "x-upload-token": "你的 API Token"
    },
    "responseUrlKey": "data[0].url"
  }
}
```

**相关接口**：

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/info` | 无 | 服务基本信息（版本号、上限、功能开关） |
| GET | `/api-docs` | 无 | API 文档 HTML 页面 |

---

## 五、P2 工程优化方向（待确认）

### 5.1 SQLite 数据库迁移

**背景**：当前使用 JSON 文件存储元数据，图片量超过 1000 张后读写性能明显下降，且不支持复杂查询。

**迁移对比**：

| 项目 | 当前（JSON） | 迁移后（SQLite） |
|------|------------|----------------|
| 图片列表查询 | 全量读取后内存过滤 | SQL WHERE + INDEX |
| 分页 | 前端内存切片 | SQL LIMIT + OFFSET |
| 排序 | Array.sort() | SQL ORDER BY |
| 搜索 | indexOf 遍历 | SQL LIKE / FTS |
| 并发写入 | 文件锁风险 | SQLite WAL 模式 |

**依赖**：引入 `better-sqlite3`（同步 API，轻量，无需额外进程）—— ⚠️ 需用户确认后才能引入。

**迁移策略**：
- 提供数据迁移脚本 `scripts/migrate-to-sqlite.js`
- 对外 API 接口保持不变，只改底层存储层
- 迁移前自动备份 JSON 文件

---

## 六、关键指标（建议跟踪）

| 指标 | v1.3 基线 | v1.4 目标 | 说明 |
|------|----------|----------|------|
| 图库列表响应时间 | 187ms（50并发） | ≤100ms（SQLite 后） | 数据库迁移收益 |
| 上传成功率 | 99% | ≥99.5% | 队列重试机制收益 |
| 误删恢复率 | 需进回收站 | ≥95%（Undo 机制） | 直接 Undo 降低操作门槛 |
| 外部工具接入数 | 0 | ≥PicGo 接入验证 | API 文档落地 |

---

*— END OF TRANSMISSION //*
*// NEON.IMG v1.4 // POWERED BY NIGHT CITY //*
