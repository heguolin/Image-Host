# 🖼️ 图床服务搭建完整指南（赛博朋克版）

> 从零开始搭建一个属于自己的图床服务，支持本地开发（Windows）+ 远程部署（Linux）。  
> **UI 风格：赛博朋克（Cyberpunk 2077 / Neo-Tokyo 霓虹美学）**

---

## 📋 目录

- [一、项目概述](#一项目概述)
- [二、技术选型](#二技术选型)
- [三、项目结构](#三项目结构)
- [四、环境准备](#四环境准备)
- [五、后端开发](#五后端开发)
- [六、前端 UI 风格设计——赛博朋克](#六前端-ui-风格设计赛博朋克)
- [七、前端代码实现](#七前端代码实现)
- [八、本地测试（Windows）](#八本地测试windows)
- [九、部署到 Linux 服务器](#九部署到-linux-服务器)
- [十、Nginx 反向代理 + HTTPS](#十nginx-反向代理--https)
- [十一、常见问题与扩展](#十一常见问题与扩展)
- [📦 一键启动备忘](#-一键启动备忘)

---

## 一、项目概述

### 1.1 功能需求

| 功能 | 说明 |
| --- | --- |
| 图片上传 | 支持拖拽、点击、粘贴上传，多文件批量上传 |
| 图片查看 | 网页画廊展示已上传图片 |
| 图片删除 | 单张或批量删除 |
| 链接生成 | 自动生成 URL / Markdown / HTML 三种格式 |
| 类型校验 | 限制图片格式（jpg/png/gif/webp/svg）与大小（≤10MB） |
| 一键复制 | 点击链接即可复制到剪贴板 |

### 1.2 流程图

```
用户浏览器
   │  ① 选择/拖入/粘贴图片
   ▼
前端 (HTML+JS) ── ② POST /api/upload ──► 后端 (Express)
                                              │
                                              ├─ ③ 校验类型/大小
                                              ├─ ④ 重命名 (timestamp + nanoid)
                                              ├─ ⑤ 写入 uploads/ 目录
                                              ├─ ⑥ 记录元数据到 images.json
                                              └─ ⑦ 返回 JSON {url, name, size}
                                              
用户浏览器 ◄── ⑧ 渲染卡片 + 提供复制/删除按钮
```

---

## 二、技术选型

| 层级 | 技术 | 理由 |
| --- | --- | --- |
| 后端 | **Node.js + Express** | 轻量、生态丰富、跨平台 |
| 文件上传 | **Multer** | Express 官方推荐的上传中间件 |
| 前端 | **原生 HTML + CSS + JS** | 无需构建工具，开箱即用 |
| 存储 | **本地文件系统** | 简单可靠，后续可平滑迁移至 OSS |
| 数据库 | **JSON 文件**（暂不需要数据库） | 基础版无需引入数据库 |
| 进程管理 | **PM2** | 生产环境守护进程 |
| 反向代理 | **Nginx** | 处理静态资源、HTTPS、限流 |

> 💡 后续若要加用户系统，推荐引入 **SQLite**（零配置）或 **MySQL**。

---

## 三、项目结构

```
image-host/
├── server/                  # 后端
│   ├── app.js               # Express 入口
│   ├── routes/
│   │   └── upload.js        # 上传/删除/列表路由
│   ├── middleware/
│   │   └── multerConfig.js  # 上传配置
│   ├── utils/
│   │   └── meta.js          # 元数据读写（JSON）
│   ├── uploads/             # 图片存储目录（运行时生成）
│   ├── data/
│   │   └── images.json      # 图片元信息
│   └── package.json
├── public/                  # 前端静态资源
│   ├── index.html
│   ├── style.css
│   └── app.js
├── .env                     # 环境变量
├── .gitignore
├── ecosystem.config.js      # PM2 配置
├── Dockerfile               # （可选）容器化
└── README.md
```

---

## 四、环境准备

### 4.1 Windows 本地环境

1. **安装 Node.js**（推荐 LTS 版本 ≥ 18）  
   下载地址：https://nodejs.org/  
   验证安装：
   ```powershell
   node -v
   npm -v
   ```

2. **安装 Git**（用于版本管理与后续部署）  
   下载地址：https://git-scm.com/

3. **代码编辑器**：推荐 **VS Code**

### 4.2 Linux 服务器环境

- 系统：Ubuntu 22.04 / Debian 12 / CentOS 7+
- 配置：1 核 1G 起步即可
- 需要：能 SSH 登录，已开放 80/443 端口

---

## 五、后端开发

### 5.1 初始化项目

```powershell
mkdir image-host
cd image-host
mkdir server public
cd server
npm init -y
npm install express@^5.2.1 multer@^2.1.1 cors@^2.8.5 dotenv@^16.4.5 nanoid@^3.3.7
npm install -D nodemon@^3.1.0
```

`server/package.json` 示例：

```json
{
  "name": "image-host",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "nanoid": "^3.3.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
```

### 5.2 入口文件 `server/app.js`

```javascript
require('dotenv').config();
const express = require('express');
const cors = require('cors');
const path = require('path');
const fs = require('fs');

const uploadRouter = require('./routes/upload');

const app = express();
const PORT = process.env.PORT || 3000;

// 确保目录存在
['uploads', 'data'].forEach(dir => {
  const p = path.join(__dirname, dir);
  if (!fs.existsSync(p)) fs.mkdirSync(p, { recursive: true });
});

app.use(cors());
app.use(express.json());

// 静态资源：前端页面
app.use(express.static(path.join(__dirname, '../public')));
// 静态资源：图片访问
app.use('/i', express.static(path.join(__dirname, 'uploads'), {
  maxAge: '7d', // 浏览器缓存 7 天
}));

// API 路由
app.use('/api', uploadRouter);

app.listen(PORT, () => {
  console.log(`🚀 图床服务已启动: http://localhost:${PORT}`);
});
```

### 5.3 上传配置 `server/middleware/multerConfig.js`

```javascript
const multer = require('multer');
const path = require('path');
const { nanoid } = require('nanoid');

const ALLOWED = ['.jpg', '.jpeg', '.png', '.gif', '.webp', '.svg'];
const MAX_SIZE = 10 * 1024 * 1024; // 10MB

const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, path.join(__dirname, '../uploads'));
  },
  filename: (req, file, cb) => {
    const ext = path.extname(file.originalname).toLowerCase();
    cb(null, `${Date.now()}-${nanoid(8)}${ext}`);
  }
});

const fileFilter = (req, file, cb) => {
  const ext = path.extname(file.originalname).toLowerCase();
  if (!ALLOWED.includes(ext)) {
    return cb(new Error(`不支持的文件类型: ${ext}`));
  }
  cb(null, true);
};

module.exports = multer({
  storage,
  fileFilter,
  limits: { fileSize: MAX_SIZE }
});
```

### 5.4 元数据工具 `server/utils/meta.js`

```javascript
const fs = require('fs');
const path = require('path');

const META_FILE = path.join(__dirname, '../data/images.json');

function readMeta() {
  if (!fs.existsSync(META_FILE)) return [];
  try {
    return JSON.parse(fs.readFileSync(META_FILE, 'utf-8'));
  } catch {
    return [];
  }
}

function writeMeta(list) {
  fs.writeFileSync(META_FILE, JSON.stringify(list, null, 2));
}

module.exports = { readMeta, writeMeta };
```

### 5.5 路由 `server/routes/upload.js`

```javascript
const express = require('express');
const fs = require('fs');
const path = require('path');
const upload = require('../middleware/multerConfig');
const { readMeta, writeMeta } = require('../utils/meta');

const router = express.Router();

// 上传（支持多文件，字段名 files）
router.post('/upload', upload.array('files', 10), (req, res) => {
  if (!req.files || req.files.length === 0) {
    return res.status(400).json({ code: 1, msg: '没有上传文件' });
  }

  const host = `${req.protocol}://${req.get('host')}`;
  const list = readMeta();

  const results = req.files.map(f => {
    const item = {
      id: f.filename,
      name: f.originalname,
      size: f.size,
      url: `${host}/i/${f.filename}`,
      uploadedAt: new Date().toISOString()
    };
    list.unshift(item);
    return item;
  });

  writeMeta(list);
  res.json({ code: 0, msg: 'ok', data: results });
});

// 列表
router.get('/list', (req, res) => {
  res.json({ code: 0, data: readMeta() });
});

// 删除
router.delete('/image/:id', (req, res) => {
  const { id } = req.params;
  const list = readMeta();
  const idx = list.findIndex(x => x.id === id);
  if (idx === -1) return res.status(404).json({ code: 1, msg: '不存在' });

  // 删除文件
  const filePath = path.join(__dirname, '../uploads', id);
  if (fs.existsSync(filePath)) fs.unlinkSync(filePath);

  list.splice(idx, 1);
  writeMeta(list);
  res.json({ code: 0, msg: '已删除' });
});

// 错误处理（Multer 等）
router.use((err, req, res, next) => {
  res.status(400).json({ code: 1, msg: err.message });
});

module.exports = router;
```

### 5.6 环境变量 `.env`

```env
PORT=3000
```

### 5.7 `.gitignore`

```
node_modules/
server/uploads/
server/data/
.env
*.log
```

---

## 六、前端 UI 风格设计——赛博朋克

> 🌃 设计灵感：**Cyberpunk 2077**、**Blade Runner 2049**、**攻壳机动队**、**Neo-Tokyo 街景**、**VAPORWAVE 80's**  
> 关键词：**霓虹**、**故障感（Glitch）**、**扫描线（Scanline）**、**HUD 界面**、**等宽字体**、**Neon Glow**

### 6.1 设计理念（Design Philosophy）

| 维度 | 描述 |
| --- | --- |
| **情绪** | 高科技 + 低生活（High Tech, Low Life）、午夜、潮湿、霓虹倒影 |
| **隐喻** | 把图床打造成一个"数据走私贩"的终端（Terminal），用户像黑客一样上传"数据包" |
| **节奏** | 暗色主导，霓虹色点缀；静态克制，交互瞬间爆发 |
| **核心动作** | "上传"被设计成 **DATA UPLINK（数据上传链路）** 的科技仪式感 |

### 6.2 色彩系统（Color Palette）

#### 主色（Primary Neon）

| 用途 | 名称 | HEX | 说明 |
| --- | --- | --- | --- |
| 主霓虹（青） | `Neon Cyan` | `#00F0FF` | 主按钮、链接、激活态、扫描线 |
| 次霓虹（品红） | `Neon Magenta` | `#FF2E97` | 强调、悬停、警告前置、删除按钮 |
| 警示霓虹（黄） | `Neon Yellow` | `#F9F002` | 提示、标签、HUD 数值 |
| 危险霓虹（红） | `Neon Red` | `#FF003C` | 删除确认、错误态 |
| 辅助霓虹（紫） | `Electric Purple` | `#B026FF` | 渐变过渡、装饰光晕 |

#### 背景（Dark Base）

| 用途 | 名称 | HEX | 说明 |
| --- | --- | --- | --- |
| 主背景 | `Void Black` | `#0A0E1A` | 页面底色，近黑略带蓝紫 |
| 次背景 | `Deep Indigo` | `#11132B` | 卡片、面板底色 |
| 表面 | `Surface` | `#1A1F3A` | 输入框、按钮底色 |
| 边框线 | `Grid Line` | `#1F2A4A` | 网格、分割线 |

#### 文本

| 用途 | HEX |
| --- | --- |
| 主文本 | `#E6F1FF`（冷白） |
| 次文本 | `#7A8CA8`（灰蓝） |
| 弱化文本 | `#3D4A6B` |

#### 渐变（Gradient）

```
霓虹光带（Header/标题/按钮）：linear-gradient(90deg, #00F0FF 0%, #FF2E97 50%, #B026FF 100%)
夜空背景渐变：              radial-gradient(ellipse at top, #11132B 0%, #0A0E1A 70%)
卡片悬停光晕：              radial-gradient(circle at 50% 0%, rgba(0,240,255,.15), transparent 70%)
```

### 6.3 字体系统（Typography）

| 角色 | 字体推荐 | 用途 |
| --- | --- | --- |
| 标题 / Logo | **Orbitron** / **Audiowide** / **Rajdhani** | 未来感几何字体（Google Fonts） |
| 等宽文本 / 数据 | **JetBrains Mono** / **Fira Code** / **Share Tech Mono** | 链接、文件名、ID 显示 |
| 正文 | **Rajdhani** / **系统无衬线** | 段落、说明文字 |
| 中文备选 | **思源黑体 / 站酷酷黑** | 中文标题 |

**字重建议**：标题 700、正文 400-500，**全大写 + letter-spacing: 2~4px** 营造 HUD 感。

### 6.4 整体布局（Layout）

```
┌────────────────────────────────────────────────────────┐
│  [▲ TOP BAR]   NEON.IMG // v1.0    ◉ ONLINE   ⌬ 12 IMG │  ← 顶部状态栏（HUD）
├────────────────────────────────────────────────────────┤
│                                                        │
│   ╔══════════════════════════════════════════════╗     │
│   ║   >> DATA UPLINK INTERFACE                   ║     │  ← 上传区（科幻终端）
│   ║                                              ║     │
│   ║        [▼]  DROP_FILE_HERE.exe               ║     │
│   ║         OR // PASTE // CLICK                 ║     │
│   ║                                              ║     │
│   ║   ──────── SCAN LINE ────────                ║     │
│   ╚══════════════════════════════════════════════╝     │
│                                                        │
│   // GALLERY · 检索到 12 条数据                         │
│   ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐                  │
│   │ IMG  │ │ IMG  │ │ IMG  │ │ IMG  │   ← 卡片网格     │
│   │ #001 │ │ #002 │ │ #003 │ │ #004 │                  │
│   └──────┘ └──────┘ └──────┘ └──────┘                  │
│                                                        │
├────────────────────────────────────────────────────────┤
│  © 2026 NEON.IMG // Powered by Night City              │  ← 底栏
└────────────────────────────────────────────────────────┘
```

- **背景**：深色 + **CSS 网格线**（伪 perspective grid）+ 远端霓虹辉光
- **整体宽度**：max-width 1200px，居中
- **间距**：8px 基础栅格，组件间距 24/32px
- **圆角**：**克制使用**（4-6px 即可），保留科技棱角感
- **边框**：1px 霓虹色 + `box-shadow` 模拟发光描边

### 6.5 核心组件设计规范

#### 6.5.1 顶部状态栏（HUD Top Bar）

- 黑底 + 底部 1px 青色光带
- 左侧：Logo `◤ NEON.IMG ◢`，霓虹渐变文字
- 右侧：**在线状态点（呼吸动画）** + 已上传图片计数（等宽字体 + 黄色霓虹）
- 字体全大写，间距 3px

#### 6.5.2 上传区（DATA UPLINK）

- **形态**：终端窗口风格，深色面板 + 双层霓虹描边（外青内品红）
- **角装饰**：四个角落用 `::before / ::after` 画 L 型霓虹角标
- **内部**：
  - 中央大号图标（向下箭头 ▼ 或 SVG 上传图标），青色发光
  - 主文案：`> DROP_FILE_HERE.exe`（等宽字体 + 闪烁光标 `_`）
  - 副文案：`// PASTE // CLICK // DRAG`，灰色等宽
- **拖拽悬停态**：边框颜色切到品红，背景透明度叠加扫描线（动画）
- **扫描线动画**：1px 青色横线从上往下循环移动（2s linear infinite）

#### 6.5.3 图片卡片（Data Card）

- **底色**：`#11132B`，1px `#1F2A4A` 边框
- **顶部**：图片预览区，比例 16:10，下方 1px 青色细线作为"数据带"
- **角标**：左上 `#001` 编号（黄色等宽字体），右上小尺寸标签（如 `.PNG`）
- **信息区**：
  - 文件名（等宽字体，超长省略号）
  - 尺寸 / 大小（次文本，灰蓝）
- **按钮组**（四个操作）：
  - `[ URL ]` `[ MD ]` `[ HTML ]` `[ DEL ]`
  - 默认透明底 + 霓虹色描边和文字
  - 悬停：背景填充对应霓虹色 + 外发光
  - `DEL` 按钮使用红色霓虹
- **悬停整卡**：上浮 4px + 青色外发光 + 顶部光带亮起

#### 6.5.4 按钮（Neon Button）

```
默认态：  透明背景 + 1px 霓虹边框 + 霓虹文字
悬停态：  霓虹底色 + 黑色文字 + 外发光 box-shadow: 0 0 12px <color>
点击态：  轻微下沉 + 内发光
禁用态：  灰度化 + 不显光
```

#### 6.5.5 Toast 提示

- 黑色半透明底（`rgba(10,14,26,.92)`）
- 左侧 3px 霓虹色竖条（成功=青、失败=红、信息=黄）
- 等宽字体，全大写，前缀 `[ SUCCESS ]` / `[ ERROR ]` / `[ INFO ]`
- 从屏幕右上滑入，3s 自动滑出

#### 6.5.6 模态框（Confirm Dialog）

- 整屏暗化遮罩 + 中央终端窗口
- 标题栏：`! CONFIRM // DELETE`（品红色）
- 内容：等宽字体询问
- 按钮：`[ CANCEL ]`（灰）/ `[ CONFIRM ]`（红霓虹）

### 6.6 视觉特效（FX）

#### 6.6.1 霓虹发光（Neon Glow）

> 用 `text-shadow` 和 `box-shadow` 多层叠加模拟真实灯管。

```css
/* 文字发光示例 */
text-shadow:
  0 0 4px  #00F0FF,
  0 0 12px #00F0FF,
  0 0 32px rgba(0,240,255,.6);

/* 边框发光示例 */
border: 1px solid #00F0FF;
box-shadow:
  0 0 8px  rgba(0,240,255,.6),
  inset 0 0 8px rgba(0,240,255,.2);
```

#### 6.6.2 扫描线（Scanlines）

- **全屏覆盖层**：一张 1px 重复的横线渐变图，整体透明度 5%-8%
- **CRT 弧光**（可选）：整体加微弱 `radial-gradient` 模拟显示器圆角暗角
- **滚动扫描线**：单独一根 2px 高的青色光带从顶到底循环

#### 6.6.3 故障效果（Glitch）

- 应用于：**Logo / 大标题 / 错误提示**
- 实现：同一文字用红/青两层伪元素错位 1-2px，配合 `clip-path` 关键帧切片抖动
- 触发：页面加载、悬停、上传成功瞬间

#### 6.6.4 背景网格（Cyber Grid）

- **方案 A**：CSS `linear-gradient` 重复 40×40 像素网格，颜色 `#1F2A4A`，透明度低
- **方案 B**：透视网格（perspective grid），底部往远处收敛，营造"网络空间"感
- 远端点缀一团 `radial-gradient` 紫色光晕模拟"赛博日出"

#### 6.6.5 微动画（Micro-interactions）

| 事件 | 效果 |
| --- | --- |
| 页面加载 | 顶部光带从左往右扫过一次 |
| 卡片入场 | 从底部淡入 + 短暂故障抖动 |
| 上传中 | 上传区显示进度条（青色霓虹），底部输出 fake terminal 日志 |
| 上传成功 | 全屏闪一次青色（80ms） + Toast 弹出 |
| 删除 | 卡片向左切片消散 + 短促数字噪点 |
| 复制 | 按钮短暂变成 `[ COPIED ✓ ]` 黄色霓虹 |
| 鼠标悬停链接 | 文字短暂故障 1 帧 |

#### 6.6.6 鼠标光标（Cursor）

- 自定义为 **方形像素光标** + 中间一个小十字（科技感）
- 悬停可点击元素时切换成 **`[ ]` 方括号瞄准框**

### 6.7 文案风格（Microcopy）

> 把功能性文案换成"黑客 / 终端 / 赛博" 用语，整体仪式感拉满。

| 场景 | 普通文案 | 赛博朋克文案 |
| --- | --- | --- |
| 站点名 | 我的图床 | `◤ NEON.IMG ◢` / `DATA_VAULT` |
| 上传提示 | 把图片拖到这里 | `> DROP_FILE_HERE.exe` |
| 副提示 | 或点击选择 | `// PASTE // CLICK // DRAG` |
| 上传中 | 上传中… | `>> UPLINK IN PROGRESS...` |
| 上传成功 | 上传成功 | `[ SUCCESS ] DATA PACKET RECEIVED` |
| 上传失败 | 上传失败 | `[ ERROR ] CONNECTION LOST // RETRY?` |
| 复制成功 | 已复制 | `[ COPIED TO CLIPBOARD ]` |
| 删除确认 | 确认删除？ | `! WARNING: PURGE THIS DATA?` |
| 删除成功 | 已删除 | `[ DATA PURGED ]` |
| 列表标题 | 我的相册 | `// GALLERY · 检索到 N 条数据` |
| 空状态 | 还没有图片 | `// NO DATA FOUND IN VAULT` |
| 页脚 | © 2026 | `© 2026 NEON.IMG // Powered by Night City` |

> 中英混排的"赛博中文"是关键：标签英文化，长句保留中文。

### 6.8 响应式断点（Responsive）

| 断点 | 设备 | 调整 |
| --- | --- | --- |
| ≥ 1200px | 桌面 | 画廊 4 列 |
| 768 - 1199px | 平板 | 画廊 3 列，HUD 顶栏简化 |
| 480 - 767px | 大手机 | 画廊 2 列，按钮组折叠为图标 |
| < 480px | 小手机 | 画廊 1 列，关闭部分动画（性能） |

**降级策略**：移动端默认关闭"扫描线动画"和"故障效果"，仅保留霓虹颜色和静态发光，避免性能/电量问题。

### 6.9 无障碍 & 性能

- **对比度**：所有文字与背景对比度 ≥ 4.5:1
- **动画**：尊重 `prefers-reduced-motion`，关闭扫描线、故障、闪烁
- **色弱友好**：不要只用颜色传达信息，按钮始终带文字标签
- **性能**：
  - 发光特效用 `box-shadow` / `filter: drop-shadow` 而非多层 PNG
  - 扫描线用纯 CSS 渐变，不引入图片
  - 长列表用 `loading="lazy"` 与 `content-visibility: auto`

### 6.10 资源清单（Assets）

| 资源 | 来源 | 备注 |
| --- | --- | --- |
| 字体 Orbitron / Rajdhani | Google Fonts | 免费可商用 |
| 字体 JetBrains Mono | https://www.jetbrains.com/mono/ | 免费 |
| 图标 | **Lucide** / **Phosphor Icons** / **Tabler** | 选线条风格，自行调色为霓虹 |
| 噪点纹理（可选） | 自制 SVG 或 https://www.transparenttextures.com/ | 增加质感 |
| 灵感参考 | Dribbble 搜索 `cyberpunk ui` / `hud interface` | 视觉打样 |

### 6.11 风格 Checklist ✅

构建完成后，对照下表自检是否"够赛博朋克"：

- [ ] 深色背景（接近黑），无任何纯白底
- [ ] 至少 2 种霓虹色作为主点缀（青 + 品红）
- [ ] 等宽字体用于所有数据/链接展示
- [ ] 标题字体为几何未来感（Orbitron 等）
- [ ] 关键文字带 `text-shadow` 发光
- [ ] 按钮 / 卡片带霓虹边框 + 外发光
- [ ] 有扫描线 / 网格 / 故障 三选其一以上的特效
- [ ] 文案英文化、全大写、带方括号或 `//` 前缀
- [ ] 顶部带 HUD 信息（计数 / 状态 / 时钟）
- [ ] 悬停 / 点击有明显的霓虹反馈

---

## 七、前端代码实现

> 下面是按第六章规范实现的**参考代码**，可直接使用。  
> 建议拆分顺序：HTML 骨架 → CSS 变量 → 背景/布局 → 组件 → 特效。

### 7.1 `public/index.html`

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>◤ NEON.IMG ◢ // DATA VAULT</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;900&family=JetBrains+Mono:wght@400;700&family=Rajdhani:wght@400;600;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <!-- 全屏扫描线覆盖层 -->
  <div class="scanlines"></div>
  <!-- 滚动扫描光带 -->
  <div class="scan-beam"></div>

  <!-- HUD 顶栏 -->
  <header class="hud">
    <div class="hud-left">
      <span class="logo glitch" data-text="◤ NEON.IMG ◢">◤ NEON.IMG ◢</span>
      <span class="version">// v1.0</span>
    </div>
    <div class="hud-right">
      <span class="status"><i class="dot"></i> ONLINE</span>
      <span class="counter">⌬ <b id="count">0</b> IMG</span>
    </div>
  </header>

  <main>
    <!-- 上传区 -->
    <section id="dropzone" class="uplink">
      <span class="corner tl"></span>
      <span class="corner tr"></span>
      <span class="corner bl"></span>
      <span class="corner br"></span>

      <h2 class="uplink-title">&gt;&gt; DATA UPLINK INTERFACE</h2>
      <div class="uplink-icon">▼</div>
      <p class="uplink-main">&gt; DROP_FILE_HERE.exe<span class="caret">_</span></p>
      <p class="uplink-sub">// PASTE // CLICK // DRAG &nbsp;·&nbsp; MAX 10MB</p>
      <input type="file" id="fileInput" multiple accept="image/*" hidden />
    </section>

    <!-- 画廊 -->
    <h3 class="section-title">// GALLERY · 检索到 <span id="count2">0</span> 条数据</h3>
    <section id="gallery" class="gallery"></section>
    <p id="empty" class="empty">// NO DATA FOUND IN VAULT</p>
  </main>

  <footer>© 2026 NEON.IMG // Powered by Night City</footer>

  <!-- Toast 容器 -->
  <div id="toastWrap" class="toast-wrap"></div>

  <!-- 模态框 -->
  <div id="modal" class="modal hidden">
    <div class="modal-box">
      <div class="modal-header">! CONFIRM // DELETE</div>
      <div class="modal-body">即将永久销毁该数据包，确认继续？</div>
      <div class="modal-footer">
        <button id="modalCancel" class="btn">[ CANCEL ]</button>
        <button id="modalOk" class="btn btn-danger">[ CONFIRM ]</button>
      </div>
    </div>
  </div>

  <script src="/app.js"></script>
</body>
</html>
```

### 7.2 `public/style.css`

```css
/* ========== 1. 变量 & 重置 ========== */
:root {
  --neon-cyan: #00F0FF;
  --neon-magenta: #FF2E97;
  --neon-yellow: #F9F002;
  --neon-red: #FF003C;
  --neon-purple: #B026FF;

  --bg-void: #0A0E1A;
  --bg-indigo: #11132B;
  --bg-surface: #1A1F3A;
  --grid-line: #1F2A4A;

  --text-main: #E6F1FF;
  --text-sub: #7A8CA8;
  --text-mute: #3D4A6B;

  --font-title: 'Orbitron', 'Rajdhani', sans-serif;
  --font-mono: 'JetBrains Mono', 'Share Tech Mono', monospace;
  --font-body: 'Rajdhani', system-ui, sans-serif;
}
* { box-sizing: border-box; margin: 0; padding: 0; }
html, body { height: 100%; }
body {
  font-family: var(--font-body);
  color: var(--text-main);
  background:
    radial-gradient(ellipse at top, #1a1d3d 0%, var(--bg-void) 70%),
    linear-gradient(var(--grid-line) 1px, transparent 1px) 0 0 / 40px 40px,
    linear-gradient(90deg, var(--grid-line) 1px, transparent 1px) 0 0 / 40px 40px,
    var(--bg-void);
  min-height: 100vh; overflow-x: hidden;
  letter-spacing: .5px;
}
img { display: block; max-width: 100%; }

/* ========== 2. 全屏特效 ========== */
.scanlines {
  position: fixed; inset: 0; pointer-events: none; z-index: 9998;
  background: repeating-linear-gradient(
    to bottom,
    rgba(255,255,255,.04) 0,
    rgba(255,255,255,.04) 1px,
    transparent 1px,
    transparent 3px
  );
  mix-blend-mode: overlay; opacity: .6;
}
.scan-beam {
  position: fixed; left: 0; right: 0; height: 2px; top: 0; z-index: 9999;
  background: linear-gradient(90deg, transparent, var(--neon-cyan), transparent);
  box-shadow: 0 0 12px var(--neon-cyan);
  animation: beamMove 6s linear infinite; pointer-events: none;
}
@keyframes beamMove {
  0%   { top: -2px; }
  100% { top: 100vh; }
}

/* ========== 3. HUD 顶栏 ========== */
.hud {
  display: flex; justify-content: space-between; align-items: center;
  padding: 14px 24px;
  background: rgba(10,14,26,.85); backdrop-filter: blur(6px);
  border-bottom: 1px solid var(--neon-cyan);
  box-shadow: 0 0 16px rgba(0,240,255,.25);
  font-family: var(--font-mono); font-size: 13px;
  text-transform: uppercase; letter-spacing: 2px;
  position: sticky; top: 0; z-index: 100;
}
.logo {
  font-family: var(--font-title); font-weight: 900; font-size: 18px;
  background: linear-gradient(90deg, var(--neon-cyan), var(--neon-magenta), var(--neon-purple));
  -webkit-background-clip: text; background-clip: text; color: transparent;
  margin-right: 12px;
}
.version { color: var(--text-sub); }
.hud-right { display: flex; gap: 24px; align-items: center; }
.status .dot {
  display: inline-block; width: 8px; height: 8px; border-radius: 50%;
  background: var(--neon-cyan); margin-right: 6px;
  box-shadow: 0 0 8px var(--neon-cyan);
  animation: breathe 1.6s ease-in-out infinite;
}
@keyframes breathe { 0%,100% { opacity: 1; } 50% { opacity: .3; } }
.counter b { color: var(--neon-yellow); text-shadow: 0 0 8px var(--neon-yellow); }

/* ========== 4. 主体 ========== */
main { max-width: 1200px; margin: 32px auto; padding: 0 24px; }

/* ========== 5. 上传区 ========== */
.uplink {
  position: relative;
  background: linear-gradient(180deg, rgba(17,19,43,.7), rgba(10,14,26,.7));
  border: 1px solid var(--neon-cyan);
  box-shadow:
    0 0 18px rgba(0,240,255,.35),
    inset 0 0 30px rgba(0,240,255,.08);
  padding: 48px 24px; text-align: center; cursor: pointer; overflow: hidden;
  transition: .2s;
}
.uplink::after {
  content: ''; position: absolute; left: 0; right: 0; top: 0; height: 2px;
  background: linear-gradient(90deg, transparent, var(--neon-cyan), transparent);
  animation: scanDown 2.4s linear infinite;
}
@keyframes scanDown { 0% { top: 0; } 100% { top: 100%; } }
.uplink.hover {
  border-color: var(--neon-magenta);
  box-shadow: 0 0 24px rgba(255,46,151,.5), inset 0 0 30px rgba(255,46,151,.12);
}
.corner {
  position: absolute; width: 18px; height: 18px;
  border: 2px solid var(--neon-magenta);
}
.corner.tl { top: 8px; left: 8px; border-right: none; border-bottom: none; }
.corner.tr { top: 8px; right: 8px; border-left: none; border-bottom: none; }
.corner.bl { bottom: 8px; left: 8px; border-right: none; border-top: none; }
.corner.br { bottom: 8px; right: 8px; border-left: none; border-top: none; }
.uplink-title {
  font-family: var(--font-mono); font-size: 12px; letter-spacing: 3px;
  color: var(--neon-cyan); margin-bottom: 24px; text-shadow: 0 0 8px var(--neon-cyan);
}
.uplink-icon {
  font-size: 56px; color: var(--neon-cyan); margin-bottom: 12px;
  text-shadow: 0 0 16px var(--neon-cyan), 0 0 32px rgba(0,240,255,.6);
  animation: floatY 2.4s ease-in-out infinite;
}
@keyframes floatY { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-6px); } }
.uplink-main {
  font-family: var(--font-mono); font-size: 22px; font-weight: 700;
  color: var(--text-main); margin-bottom: 8px;
}
.caret {
  display: inline-block; color: var(--neon-cyan);
  animation: blink 1s steps(2) infinite;
}
@keyframes blink { 50% { opacity: 0; } }
.uplink-sub { color: var(--text-sub); font-family: var(--font-mono); font-size: 12px; letter-spacing: 2px; }

/* ========== 6. 区块标题 ========== */
.section-title {
  margin: 40px 0 16px;
  font-family: var(--font-mono); font-size: 14px;
  color: var(--neon-cyan); text-shadow: 0 0 6px rgba(0,240,255,.6);
  letter-spacing: 2px; text-transform: uppercase;
}

/* ========== 7. 卡片网格 ========== */
.gallery {
  display: grid; gap: 18px;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
}
.card {
  position: relative; background: var(--bg-indigo);
  border: 1px solid var(--grid-line);
  transition: .25s; overflow: hidden;
  animation: cardIn .4s ease both;
}
@keyframes cardIn {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}
.card:hover {
  border-color: var(--neon-cyan); transform: translateY(-4px);
  box-shadow: 0 8px 24px rgba(0,240,255,.25);
}
.card .preview {
  aspect-ratio: 16/10; background: #000; overflow: hidden;
  border-bottom: 1px solid var(--neon-cyan);
  box-shadow: 0 1px 8px rgba(0,240,255,.4);
}
.card .preview img { width: 100%; height: 100%; object-fit: cover; }
.card .badge-id {
  position: absolute; top: 8px; left: 8px;
  background: rgba(10,14,26,.8); color: var(--neon-yellow);
  font-family: var(--font-mono); font-size: 11px; padding: 2px 6px;
  border: 1px solid var(--neon-yellow); text-shadow: 0 0 4px var(--neon-yellow);
}
.card .badge-ext {
  position: absolute; top: 8px; right: 8px;
  background: rgba(10,14,26,.8); color: var(--neon-cyan);
  font-family: var(--font-mono); font-size: 11px; padding: 2px 6px;
  border: 1px solid var(--neon-cyan);
}
.card .info { padding: 12px; }
.card .info .name {
  font-family: var(--font-mono); font-size: 12px; color: var(--text-main);
  white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
  margin-bottom: 4px;
}
.card .info .meta {
  font-family: var(--font-mono); font-size: 11px; color: var(--text-sub);
  margin-bottom: 10px;
}
.card .btns { display: grid; grid-template-columns: repeat(4,1fr); gap: 6px; }

/* ========== 8. 按钮 ========== */
.btn {
  font-family: var(--font-mono); font-size: 11px; letter-spacing: 1px;
  padding: 6px 4px; background: transparent;
  border: 1px solid var(--neon-cyan); color: var(--neon-cyan);
  cursor: pointer; transition: .15s; text-transform: uppercase;
}
.btn:hover {
  background: var(--neon-cyan); color: var(--bg-void);
  box-shadow: 0 0 12px var(--neon-cyan);
}
.btn-danger { border-color: var(--neon-red); color: var(--neon-red); }
.btn-danger:hover { background: var(--neon-red); color: #fff; box-shadow: 0 0 12px var(--neon-red); }

/* ========== 9. 空状态 / 页脚 ========== */
.empty {
  text-align: center; padding: 40px; color: var(--text-mute);
  font-family: var(--font-mono); font-size: 13px;
}
.empty.hidden { display: none; }
footer {
  text-align: center; padding: 24px; margin-top: 40px;
  border-top: 1px solid var(--grid-line);
  color: var(--text-mute); font-family: var(--font-mono); font-size: 11px;
  letter-spacing: 2px;
}

/* ========== 10. Toast ========== */
.toast-wrap {
  position: fixed; top: 80px; right: 24px; z-index: 200;
  display: flex; flex-direction: column; gap: 10px;
}
.toast {
  min-width: 240px; padding: 10px 16px;
  background: rgba(10,14,26,.92); color: var(--text-main);
  border-left: 3px solid var(--neon-cyan);
  font-family: var(--font-mono); font-size: 12px; letter-spacing: 1px;
  box-shadow: 0 0 12px rgba(0,240,255,.3);
  animation: toastIn .3s ease both;
}
.toast.error { border-color: var(--neon-red); box-shadow: 0 0 12px rgba(255,0,60,.4); }
.toast.info  { border-color: var(--neon-yellow); }
@keyframes toastIn {
  from { transform: translateX(120%); opacity: 0; }
  to   { transform: translateX(0); opacity: 1; }
}

/* ========== 11. 模态框 ========== */
.modal {
  position: fixed; inset: 0; background: rgba(0,0,0,.7);
  display: flex; align-items: center; justify-content: center; z-index: 300;
}
.modal.hidden { display: none; }
.modal-box {
  background: var(--bg-indigo);
  border: 1px solid var(--neon-magenta);
  box-shadow: 0 0 24px rgba(255,46,151,.5);
  min-width: 320px; max-width: 90vw;
}
.modal-header {
  padding: 10px 16px; background: rgba(255,46,151,.1);
  border-bottom: 1px solid var(--neon-magenta);
  font-family: var(--font-mono); font-size: 12px; letter-spacing: 2px;
  color: var(--neon-magenta); text-shadow: 0 0 6px var(--neon-magenta);
}
.modal-body {
  padding: 20px 16px; font-family: var(--font-mono); font-size: 13px;
  color: var(--text-main);
}
.modal-footer {
  display: flex; gap: 10px; justify-content: flex-end;
  padding: 12px 16px; border-top: 1px solid var(--grid-line);
}

/* ========== 12. Glitch ========== */
.glitch { position: relative; }
.glitch::before, .glitch::after {
  content: attr(data-text); position: absolute; left: 0; top: 0;
  width: 100%; overflow: hidden;
}
.glitch::before { color: var(--neon-magenta); transform: translate(1px,0); clip-path: inset(0 0 80% 0); }
.glitch::after  { color: var(--neon-cyan);    transform: translate(-1px,0); clip-path: inset(80% 0 0 0); }

/* ========== 13. 响应式 & 减弱动效 ========== */
@media (max-width: 767px) {
  .hud { flex-direction: column; gap: 8px; padding: 12px; }
  .gallery { grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); }
  .uplink-icon { font-size: 40px; }
  .uplink-main { font-size: 18px; }
}
@media (prefers-reduced-motion: reduce) {
  .scan-beam, .uplink::after, .uplink-icon, .caret, .status .dot { animation: none !important; }
}
```

### 7.3 `public/app.js`

```javascript
const dropzone  = document.getElementById('dropzone');
const fileInput = document.getElementById('fileInput');
const gallery   = document.getElementById('gallery');
const empty     = document.getElementById('empty');
const count     = document.getElementById('count');
const count2    = document.getElementById('count2');
const toastWrap = document.getElementById('toastWrap');
const modal     = document.getElementById('modal');
const modalOk   = document.getElementById('modalOk');
const modalCancel = document.getElementById('modalCancel');

let pendingDelete = null;

// ---------- 工具 ----------
function toast(msg, type = 'success') {
  const prefix = { success: '[ SUCCESS ]', error: '[ ERROR ]', info: '[ INFO ]' }[type] || '[ INFO ]';
  const el = document.createElement('div');
  el.className = `toast ${type}`;
  el.textContent = `${prefix} ${msg}`;
  toastWrap.appendChild(el);
  setTimeout(() => el.remove(), 3000);
}

async function copy(text) {
  try {
    await navigator.clipboard.writeText(text);
    toast('COPIED TO CLIPBOARD');
  } catch {
    toast('COPY FAILED', 'error');
  }
}

function formatSize(bytes) {
  if (bytes < 1024) return bytes + ' B';
  if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + ' KB';
  return (bytes / 1024 / 1024).toFixed(2) + ' MB';
}

function updateCount(n) {
  count.textContent = n;
  count2.textContent = n;
  empty.classList.toggle('hidden', n > 0);
}

// ---------- 渲染 ----------
function renderCard(item, index) {
  const ext = (item.name.split('.').pop() || '').toUpperCase();
  const card = document.createElement('div');
  card.className = 'card';
  card.dataset.id = item.id;
  card.innerHTML = `
    <div class="preview">
      <img src="${item.url}" alt="${item.name}" loading="lazy" />
    </div>
    <span class="badge-id">#${String(index + 1).padStart(3, '0')}</span>
    <span class="badge-ext">.${ext}</span>
    <div class="info">
      <div class="name" title="${item.name}">${item.name}</div>
      <div class="meta">${formatSize(item.size)}</div>
      <div class="btns">
        <button class="btn" data-act="url">URL</button>
        <button class="btn" data-act="md">MD</button>
        <button class="btn" data-act="html">HTML</button>
        <button class="btn btn-danger" data-act="del">DEL</button>
      </div>
    </div>
  `;
  card.addEventListener('click', e => {
    const act = e.target.dataset.act;
    if (!act) return;
    if (act === 'url')  copy(item.url);
    if (act === 'md')   copy(`![${item.name}](${item.url})`);
    if (act === 'html') copy(`<img src="${item.url}" alt="${item.name}" />`);
    if (act === 'del')  askDelete(item.id, card);
  });
  return card;
}

function rerender(list) {
  gallery.innerHTML = '';
  list.forEach((item, i) => gallery.appendChild(renderCard(item, i)));
  updateCount(list.length);
}

// ---------- API ----------
async function loadList() {
  try {
    const res = await fetch('/api/list');
    const json = await res.json();
    rerender(json.data || []);
  } catch {
    toast('LOAD LIST FAILED', 'error');
  }
}

async function uploadFiles(files) {
  if (!files || files.length === 0) return;
  const fd = new FormData();
  [...files].forEach(f => fd.append('files', f));
  toast(`UPLINK IN PROGRESS... (${files.length})`, 'info');
  try {
    const res = await fetch('/api/upload', { method: 'POST', body: fd });
    const json = await res.json();
    if (json.code === 0) {
      toast('DATA PACKET RECEIVED');
      loadList();
    } else {
      toast(json.msg || 'UPLOAD FAILED', 'error');
    }
  } catch {
    toast('CONNECTION LOST // RETRY?', 'error');
  }
}

function askDelete(id, card) {
  pendingDelete = { id, card };
  modal.classList.remove('hidden');
}

async function doDelete() {
  if (!pendingDelete) return;
  const { id, card } = pendingDelete;
  try {
    const res = await fetch('/api/image/' + id, { method: 'DELETE' });
    const json = await res.json();
    if (json.code === 0) {
      card.remove();
      toast('DATA PURGED');
      loadList();
    } else {
      toast(json.msg, 'error');
    }
  } catch {
    toast('DELETE FAILED', 'error');
  } finally {
    pendingDelete = null;
    modal.classList.add('hidden');
  }
}

// ---------- 事件 ----------
dropzone.addEventListener('click', () => fileInput.click());
fileInput.addEventListener('change', e => uploadFiles(e.target.files));

['dragenter', 'dragover'].forEach(ev =>
  dropzone.addEventListener(ev, e => {
    e.preventDefault();
    dropzone.classList.add('hover');
  })
);
['dragleave', 'drop'].forEach(ev =>
  dropzone.addEventListener(ev, e => {
    e.preventDefault();
    dropzone.classList.remove('hover');
  })
);
dropzone.addEventListener('drop', e => uploadFiles(e.dataTransfer.files));

document.addEventListener('paste', e => {
  const files = [...e.clipboardData.items]
    .filter(i => i.type.startsWith('image/'))
    .map(i => i.getAsFile());
  if (files.length) uploadFiles(files);
});

modalOk.addEventListener('click', doDelete);
modalCancel.addEventListener('click', () => {
  pendingDelete = null;
  modal.classList.add('hidden');
});

// 启动
loadList();
```

---

## 八、本地测试（Windows）

```powershell
cd image-host\server
npm run dev
```

浏览器打开：**http://localhost:3000**

测试项：
- ✅ 拖拽上传 / 点击上传 / 粘贴上传
- ✅ 复制 URL、Markdown、HTML 链接
- ✅ 删除单张图片（弹出赛博朋克确认框）
- ✅ 上传超大文件（>10MB）应被拒绝
- ✅ 上传非图片文件应被拒绝
- ✅ **视觉验收**：对照 6.11 Checklist 逐项检查
- ✅ **动效验收**：开启系统"减少动效"后页面不再闪烁
- ✅ **响应式验收**：DevTools 切换到 iPhone / iPad 视图

---

## 九、部署到 Linux 服务器

### 9.1 在服务器安装 Node.js（以 Ubuntu 为例）

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
node -v && npm -v
```

### 9.2 上传代码到服务器

**方式 A：Git（推荐）**

```bash
# 本地
git init
git add .
git commit -m "init"
git remote add origin git@github.com:你的用户名/image-host.git
git push -u origin main

# 服务器
cd /opt
sudo git clone https://github.com/你的用户名/image-host.git
sudo chown -R $USER:$USER image-host
cd image-host/server
npm install --production
```

**方式 B：scp 直接上传**

```powershell
# Windows PowerShell
scp -r image-host user@服务器IP:/opt/
```

### 9.3 使用 PM2 守护进程

```bash
sudo npm install -g pm2

cd /opt/image-host/server
pm2 start app.js --name image-host
pm2 save                # 保存当前进程列表
pm2 startup             # 设置开机自启（按提示执行返回的命令）

# 常用命令
pm2 status              # 查看状态
pm2 logs image-host     # 查看日志
pm2 restart image-host  # 重启
pm2 stop image-host     # 停止
```

或者使用配置文件 `ecosystem.config.js`：

```javascript
module.exports = {
  apps: [{
    name: 'image-host',
    script: './server/app.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '300M',
    env: { NODE_ENV: 'production', PORT: 3000 }
  }]
};
```

启动：`pm2 start ecosystem.config.js`

### 9.4 防火墙

```bash
# Ubuntu (ufw)
sudo ufw allow 80
sudo ufw allow 443
sudo ufw allow 22
sudo ufw enable

# CentOS (firewalld)
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

> 云服务器还需在控制台的**安全组**中放行 80/443 端口。

---

## 十、Nginx 反向代理 + HTTPS

### 10.1 安装 Nginx

```bash
sudo apt install -y nginx
```

### 10.2 配置 `/etc/nginx/sites-available/image-host`

```nginx
server {
    listen 80;
    server_name img.yourdomain.com;   # 改成你的域名

    client_max_body_size 20M;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 直接由 Nginx 提供图片静态文件，性能更好
    location /i/ {
        alias /opt/image-host/server/uploads/;
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
```

启用并重载：

```bash
sudo ln -s /etc/nginx/sites-available/image-host /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### 10.3 申请免费 HTTPS（Let's Encrypt）

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d img.yourdomain.com
```

按提示输入邮箱、同意条款，Certbot 会自动改写 Nginx 配置并配置自动续期。

验证续期：

```bash
sudo certbot renew --dry-run
```

---

## 十一、常见问题与扩展

### 11.1 常见问题

| 问题 | 解决方案 |
| --- | --- |
| 上传 413 错误 | 调大 Nginx `client_max_body_size` |
| 跨域被拦截 | 已用 `cors()`，若仍报错检查反向代理头 |
| 图片打不开 | 检查 `uploads/` 目录权限 `chmod -R 755 uploads` |
| PM2 重启后图片丢失 | 确认未把 `uploads/` 放在临时目录或被 `.gitignore` 误删 |
| 上传速度慢 | 启用 Nginx gzip、HTTP/2，或换 CDN |
| 霓虹特效在低端机卡顿 | 用 `@media (prefers-reduced-motion)` 或检测设备性能后关闭动画 |
| 谷歌字体加载慢 | 国内可改用字体 CDN（如 `fonts.font.im`）或自托管字体文件 |

### 11.2 后续扩展建议

按优先级排列：

1. **🔐 鉴权**：加上传密钥防止盗刷  
   ```javascript
   router.use('/upload', (req, res, next) => {
     if (req.headers['x-token'] !== process.env.UPLOAD_TOKEN)
       return res.status(401).json({ code: 1, msg: '未授权' });
     next();
   });
   ```
2. **🖼️ 图片压缩**：使用 [`sharp`](https://github.com/lovell/sharp) 自动生成缩略图  
3. **☁️ 接入对象存储**：把 `multer.diskStorage` 换成 `multer-s3`，无缝迁移到 S3/OSS  
4. **📊 数据库化**：用 SQLite 替代 JSON，支持搜索、分页、标签  
5. **👥 多用户系统**：JWT 鉴权 + 用户私有相册  
6. **🐳 Docker 化部署**：  
   ```dockerfile
   FROM node:20-alpine
   WORKDIR /app
   COPY server/package*.json ./
   RUN npm install --production
   COPY server ./
   COPY public ../public
   EXPOSE 3000
   CMD ["node", "app.js"]
   ```
7. **🛡️ 安全增强**：限流（`express-rate-limit`）、文件内容嗅探（`file-type`）、防盗链（Nginx referer 校验）  
8. **🎨 多主题切换**：基于 CSS 变量做 `cyberpunk / minimal / vaporwave` 主题切换  
9. **🎵 沉浸增强**：可选的赛博朋克 lo-fi 背景音、上传成功的电子音效

---

## 📦 一键启动备忘

**Windows 开发：**
```powershell
cd server
npm install
npm run dev
# 打开 http://localhost:3000
```

**Linux 部署：**
```bash
cd /opt/image-host/server
npm install --production
pm2 start app.js --name image-host
pm2 save && pm2 startup
sudo systemctl reload nginx
```

---

> ✍️ 文档版本：**v2.1**（MVP 验收通过 · 依赖版本锁定 · 新增毛玻璃视频背景）  
> 📅 最后更新：2026-05-26  
> 🎯 适用阶段：基础版图床 MVP，可平滑演进到生产级  
> 🌃 视觉主题：Cyberpunk · Neon · Night City

**Wake up, samurai. We have images to host. 🌆**
